---
ng-advanced-table: patch
---

Trim the maintainer guidance in `AGENTS.md` to the rules that are not discoverable from the code or enforced by tooling: release policy, workspace tooling, the entry-point and element layering graphs, package boundaries, library design invariants, documentation ownership, testing shape, accessibility, and browser support. Removed the narrative explanations, file-by-file inventories, and implementation restatements that duplicated lint, build, and size checks or the code itself.
