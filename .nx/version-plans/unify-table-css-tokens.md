---
ng-advanced-table: minor
---

Unify the bundled table, companion control, and render-metrics CSS theme contract around the shared `--nat-table-*` semantic palette.

Document the published theming API with light and dark override examples, remove the legacy shorthand CSS variable fallbacks, add table stacking tokens, and deduplicate pagination control styles so page-size and pager controls share one CSS source.
