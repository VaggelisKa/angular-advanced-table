---
ng-advanced-table: minor
---

Add an `enableReordering` table-surface flag, disabled by default, that opts tables into column drag/drop, keyboard reordering, move-column metadata, reorder instructions, and grab affordances.
