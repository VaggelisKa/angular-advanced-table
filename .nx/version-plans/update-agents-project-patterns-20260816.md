---
ng-advanced-table: patch
---

Document maintainer guidance learned from recent merged work: nightly snapshots must derive their prerelease base from the highest pending version-plan bump without consuming release plans, MCP request/response bounds must stay in place, virtualization remains an engine-owned optional entry-point behavior, remote windowing keeps one loaded window over logical coordinates, and consumer-controlled table value traversals must remain bounded and cycle-safe.
