---
ng-advanced-table: patch
---

Remove the default hover translate/lift effect (`translateY(-1px)`) from table header sort buttons and menu buttons in `withNatTableHeaderActions`. Header cells no longer shift vertically on hover by default.
