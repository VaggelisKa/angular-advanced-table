---
ng-advanced-table-ui: minor
---

Make header actions idempotent in reactive column builders, add per-column `meta.headerActions` opt-out and overrides, resolve header labels at render time, and localize the pin overflow menu through `NatTableAccessibilityHeaderActionLabels.pinMenu`.
