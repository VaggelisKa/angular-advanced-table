---
ng-advanced-table-ui: minor
---

Deprecate `NatTableSearch` (use `NatToolbarSearch`) and `NatTableColumnVisibility` (use `NatToolbarView`). Both remain functional; removal planned for the next major.
