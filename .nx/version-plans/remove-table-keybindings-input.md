---
ng-advanced-table: patch
---

Remove the redundant `keybindings` input from the core `<nat-table>` component, consolidating custom shortcut configuration to `<nat-table-surface>` and `NatTableService.surfaceKeybindings`.
