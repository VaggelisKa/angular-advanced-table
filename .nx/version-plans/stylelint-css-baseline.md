---
ng-advanced-table: patch
---

Reformat component CSS to the Stylelint baseline (lint-suite preset) and replace the deprecated `clip` visually-hidden rule with the modern `clip-path: inset(50%)` recipe.
