---
ng-advanced-table: patch
---

Document the package layering rule and remove the stale core-library path mapping to the companion UI package so the core table package stays independent from companion packages.
