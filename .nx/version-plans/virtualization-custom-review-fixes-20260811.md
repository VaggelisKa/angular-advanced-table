---
ng-advanced-table: patch
---

Harden custom-engine row virtualization by normalizing malformed strategy geometry, preserving accessible focus across body-state and row-model changes, isolating focus recovery per table, correcting grid-start and visual Page navigation, revalidating unbounded regions after async data and resizes, and comparing arbitrary stable row IDs without delimiter collisions. Document the low-level custom-adapter responsibility boundary and lock the behavior with rendered and browser regressions.
