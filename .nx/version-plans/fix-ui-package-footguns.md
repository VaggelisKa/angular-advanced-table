---
ng-advanced-table-ui: patch
---

Declare the core table package as a peer of `ng-advanced-table-ui`, dedupe page-size options before rendering controls, harden surface state diffing for non-JSON filter values, add Safari 16-safe CSS fallbacks for companion UI styles, and lock the public sort-indicator context to the internal contract.
