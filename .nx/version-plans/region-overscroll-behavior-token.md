---
ng-advanced-table: minor
---

Expose `--nat-table-region-overscroll-behavior-y` so consumers can configure vertical overscroll containment on the table scroll region without deep style overrides.
