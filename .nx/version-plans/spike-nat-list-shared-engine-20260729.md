---
ng-advanced-table: minor
---

Add `NatList` (`nat-list`) spike component under `src/list/`, exported from the core entry point, proving the shared table engine (`NatTableState`) can drive a list renderer — sorting, column order, visibility, and data lifecycle states — with spec coverage and a showcase table-to-list toggle example. Spike API, subject to change or removal.
