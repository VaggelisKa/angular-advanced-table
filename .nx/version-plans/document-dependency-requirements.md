---
ng-advanced-table: minor
---

Ship `@tanstack/angular-table` as a runtime dependency while keeping Angular framework, Aria, and CDK packages as required peers. Document the resolved published ranges plus required Angular peer install requirements.
