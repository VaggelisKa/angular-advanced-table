---
ng-advanced-table: minor
---

Render the opt-in pinned-column divider shadow as a single soft, seamless fade off each pinned edge instead of stacked zero-blur bands, and add a shared `--nat-table-pinned-edge-shadow` token that overrides the whole fade for both pinned zones while keeping the one-pixel divider.
