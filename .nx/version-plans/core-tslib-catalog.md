---
ng-advanced-table: patch
---

Source `ng-advanced-table`'s `tslib` dependency from the shared pnpm catalog so it matches the companion packages. This raises the published `tslib` floor from `^2.3.0` to `^2.8.1`.
