---
'ng-advanced-table': minor
'ng-advanced-table-utils': minor
---

Use TanStack `size`, `minSize`, and `maxSize` as the rendered column sizing API and add `maxSize` support to the render-metrics column helper.
