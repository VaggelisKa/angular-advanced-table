/* eslint-disable max-lines -- generated docs HTML registry */
/* This file is generated by tools/generate-showcase-docs.mjs. */

export type DocsHtmlHeading = {
  readonly depth: number;
  readonly id: string;
};

export type DocsHtmlEntry = {
  readonly html: string;
  readonly headings: readonly DocsHtmlHeading[];
};

export const docsHtmlRegistry = {
  '/docs/accessibility.md': {
    html: '<p>Accessible table authoring is part of the table API. The core table handles grid roles, keyboard movement, state rows, live announcements, row selection attributes, and companion-control wiring. Your app still owns product copy, custom cells, dialogs, menus, and domain workflow accessibility.</p>\n<h2>Minimum Checklist</h2>\n<p>Before shipping a table, verify this list:</p>\n<table>\n<thead>\n<tr>\n<th>Requirement</th>\n<th>Where to set it</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Table has a clear accessible name</td>\n<td><code>caption</code> or <code>accessibleName</code></td>\n</tr>\n<tr>\n<td>Every data column has a human label</td>\n<td><code>columnDef.meta.label</code></td>\n</tr>\n<tr>\n<td>One identifying column is a row header</td>\n<td><code>columnDef.meta.rowHeader: true</code></td>\n</tr>\n<tr>\n<td>Icon-only or utility headers remain named</td>\n<td><code>columnDef.meta.hiddenHeaderLabel</code></td>\n</tr>\n<tr>\n<td>Numeric cells align predictably</td>\n<td><code>columnDef.meta.align: &#39;end&#39;</code></td>\n</tr>\n<tr>\n<td>State rows have visible, meaningful text</td>\n<td><code>natTableLoading</code>, <code>natTableEmpty</code>, <code>natTableError</code> templates or <code>accessibilityText</code></td>\n</tr>\n<tr>\n<td>Custom cell controls have names</td>\n<td>App-owned <code>aria-label</code>, visible text, or design-system labels</td>\n</tr>\n<tr>\n<td>Interactive cells integrate with grid focus</td>\n<td><code>ngGridCellWidget</code> on the real focusable element</td>\n</tr>\n<tr>\n<td>Generated UI copy is localized</td>\n<td><code>provideNatTableLocales</code>, <code>provideNatTableControlsLocales</code>, <code>provideNatTableRenderMetricsLocales</code></td>\n</tr>\n<tr>\n<td>Enabled reordering and resizing have keyboard instructions</td>\n<td><code>accessibilityText.reorderKeyboardInstructions</code>, <code>accessibilityText.resizeKeyboardInstructions</code></td>\n</tr>\n</tbody></table>\n<h2>Table Name</h2>\n<p>Use <code>caption</code> when a visible table title belongs inside the grid. Use <code>accessibleName</code> when the page already has visible context and an extra caption would be redundant.</p>\n<pre><code class="language-html">&lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; caption=&quot;Open positions&quot; /&gt;\n</code></pre>\n<pre><code class="language-html">&lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n</code></pre>\n<p>A visible caption names the rendered grid. <code>accessibleName</code> remains the required captionless fallback for authoring.</p>\n<h2>Column Metadata</h2>\n<p>Provide <code>meta.label</code> for every column. Mark the primary identifying column as the row header, and align numeric values to the end edge.</p>\n<pre><code class="language-ts">import type { ColumnDef } from &#39;ng-advanced-table&#39;;\n\nreadonly columns: ColumnDef&lt;PositionRow&gt;[] = [\n  {\n    accessorKey: &#39;symbol&#39;,\n    header: &#39;Symbol&#39;,\n    meta: { label: &#39;Symbol&#39;, rowHeader: true },\n  },\n  {\n    accessorKey: &#39;marketValue&#39;,\n    header: &#39;Market value&#39;,\n    meta: { label: &#39;Market value&#39;, align: &#39;end&#39; },\n    cell: (context) =&gt; currency.format(context.getValue&lt;number&gt;()),\n  },\n];\n</code></pre>\n<p><code>meta.label</code> is used by summaries, live announcements, companion controls, header actions, selection/export helpers, and custom sort indicators. It is especially important when the header is a component, function, icon, or non-human id.</p>\n<h2>Hidden Header Labels</h2>\n<p>Use <code>hiddenHeaderLabel</code> for compact utility columns where visible text would add clutter but assistive technology still needs a header label.</p>\n<pre><code class="language-ts">{\n  id: &#39;actions&#39;,\n  header: &#39;Actions&#39;,\n  enableSorting: false,\n  enableGlobalFilter: false,\n  enableHiding: false,\n  meta: {\n    label: &#39;Actions&#39;,\n    hiddenHeaderLabel: &#39;Row actions&#39;,\n    align: &#39;end&#39;,\n  },\n  cell: (context) =&gt;\n    flexRenderComponent(RowActionsMenu, {\n      inputs: { row: context.row.original },\n    }),\n}\n</code></pre>\n<p>The table renders hidden header labels as screen-reader-only text for both primitive and non-primitive headers, including columns wrapped with <code>withNatTableHeaderActions(...)</code>.</p>\n<h2>Table-Specific Copy</h2>\n<p>Use <code>accessibilityText</code> for product-specific descriptions, state copy, and live-announcement formatters.</p>\n<pre><code class="language-ts">import type { NatTableAccessibilityText } from &#39;ng-advanced-table&#39;;\n\nreadonly accessibilityText: NatTableAccessibilityText = {\n  description: &#39;Sortable table of open positions with a sticky symbol column.&#39;,\n  emptyState: &#39;No positions match the current filters.&#39;,\n  loadingState: &#39;Loading positions.&#39;,\n  errorState: &#39;Positions could not be loaded.&#39;,\n  reorderKeyboardInstructions:\n    &#39;Use Control+Shift+Arrow keys to move columns. On macOS, use Command+Shift+Arrow keys.&#39;,\n  resizeKeyboardInstructions:\n    &#39;Use Alt+Left and Alt+Right to resize the focused column. Use Alt+Home and Alt+End for bounds.&#39;,\n  tableSummary: ({ visibleRowsText, totalRowsText, pageText, pageCountText }) =&gt;\n    `${visibleRowsText} of ${totalRowsText} rows visible. Page ${pageText} of ${pageCountText}.`,\n  sortingChange: ({ columnLabel, sortState }) =&gt;\n    columnLabel ? `${columnLabel} sorted ${sortState}.` : &#39;Sorting cleared.&#39;,\n};\n</code></pre>\n<pre><code class="language-html">&lt;nat-table-surface [accessibilityText]=&quot;accessibilityText&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p>Use table-specific copy for the instance. Use locale providers for common generated labels.</p>\n<h2>Localization Providers</h2>\n<p>Generated copy resolves from locale providers. Add only the providers for packages you use.</p>\n<pre><code class="language-ts">import { ApplicationConfig } from &#39;@angular/core&#39;;\nimport { provideNatTableLocales, provideNatTableControlsLocales, provideNatTableRenderMetricsLocales } from &#39;ng-advanced-table/locale&#39;;\n\nexport const appConfig: ApplicationConfig = {\n  providers: [provideNatTableLocales(), provideNatTableControlsLocales(), provideNatTableRenderMetricsLocales()]\n};\n</code></pre>\n<p>When locale changes at runtime, pass the active locale to the surface and rebuild translated column definitions from the same translation source.</p>\n<pre><code class="language-ts">import type { ColumnDef } from &#39;ng-advanced-table&#39;;\n\nreadonly localeId = signal&lt;&#39;en&#39; | &#39;da&#39;&gt;(&#39;en&#39;);\nreadonly copy = computed(() =&gt; translations[this.localeId()]);\n\nreadonly columns = computed&lt;ColumnDef&lt;OrderRow&gt;[]&gt;(() =&gt; {\n  const labels = this.copy();\n\n  return [\n    {\n      accessorKey: &#39;id&#39;,\n      header: labels.order,\n      meta: { label: labels.order, rowHeader: true },\n    },\n    {\n      accessorKey: &#39;total&#39;,\n      header: labels.total,\n      meta: { label: labels.total, align: &#39;end&#39; },\n    },\n  ];\n});\n</code></pre>\n<pre><code class="language-html">&lt;nat-table-surface [locale]=&quot;localeId()&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns()&quot; [accessibleName]=&quot;copy().ordersTable&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p>Do not rely on placeholder text as a search input&#39;s only accessible label. Do not echo raw semantic tokens such as <code>ascending</code>, <code>pin</code>, or <code>hidden</code> directly into localized copy unless those tokens are translated for users.</p>\n<h2>State Rows</h2>\n<p>Use <code>dataStatus</code> and state templates for loading, empty, and error rows. The table owns row placement; your app owns fetching, retrying, and error classification.</p>\n<pre><code class="language-html">&lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; [dataStatus]=&quot;status()&quot; [error]=&quot;error()&quot; accessibleName=&quot;Open positions&quot;&gt;\n  &lt;ng-template natTableLoading&gt;\n    &lt;strong&gt;Loading positions&lt;/strong&gt;\n    &lt;span&gt;Fetching the latest open positions.&lt;/span&gt;\n  &lt;/ng-template&gt;\n\n  &lt;ng-template natTableEmpty let-filtered=&quot;filtered&quot;&gt;\n    &lt;strong&gt;No positions found&lt;/strong&gt;\n    &lt;span&gt; {{ filtered ? &#39;No rows match the active filters.&#39; : &#39;There are no open positions.&#39; }} &lt;/span&gt;\n  &lt;/ng-template&gt;\n\n  &lt;ng-template natTableError let-error&gt;\n    &lt;strong&gt;Positions unavailable&lt;/strong&gt;\n    &lt;button type=&quot;button&quot; (click)=&quot;reload()&quot;&gt;Retry&lt;/button&gt;\n  &lt;/ng-template&gt;\n&lt;/nat-table&gt;\n</code></pre>\n<p>Custom state templates should include visible text or accessible names that make sense without color or icons. Focusable controls inside state templates can be normal buttons, links, or inputs.</p>\n<h2>Custom Interactive Cells</h2>\n<p>Use <code>flexRenderComponent(...)</code> for Angular component cells. Put <code>ngGridCellWidget</code> from <code>@angular/aria/grid</code> on the real focusable element inside the custom component.</p>\n<pre><code class="language-ts">import { Component, input, output } from &#39;@angular/core&#39;;\nimport { GridCellWidget } from &#39;@angular/aria/grid&#39;;\nimport { flexRenderComponent } from &#39;ng-advanced-table&#39;;\n\n@Component({\n  selector: &#39;app-trade-button&#39;,\n  imports: [GridCellWidget],\n  template: ` &lt;button type=&quot;button&quot; ngGridCellWidget [attr.aria-label]=&quot;accessibleName()&quot; (click)=&quot;pressed.emit()&quot;&gt;Trade&lt;/button&gt; `\n})\nexport class TradeButton {\n  readonly accessibleName = input.required&lt;string&gt;();\n  readonly pressed = output&lt;void&gt;();\n}\n</code></pre>\n<pre><code class="language-ts">{\n  id: &#39;trade&#39;,\n  header: &#39;Trade&#39;,\n  enableSorting: false,\n  enableGlobalFilter: false,\n  meta: { label: &#39;Trade&#39;, hiddenHeaderLabel: &#39;Trade actions&#39; },\n  cell: (context) =&gt;\n    flexRenderComponent(TradeButton, {\n      inputs: {\n        accessibleName: `Trade ${context.row.original.symbol}`,\n      },\n      outputs: {\n        pressed: () =&gt; this.openTradeTicket(context.row.original.id),\n      },\n    }),\n}\n</code></pre>\n<p><code>(rowActivate)</code> is for row-level primary actions. It ignores events from interactive descendants such as buttons, links, inputs, menu items, and contenteditable regions.</p>\n<h2>Selection Accessibility</h2>\n<p>Row selection is enabled with <code>[enableRowSelection]=&quot;true&quot;</code> and optionally a generated checkbox column from <code>withNatTableSelectionColumn(...)</code>.</p>\n<pre><code class="language-ts">readonly columns = withNatTableSelectionColumn(baseColumns, {\n  label: &#39;Selection&#39;,\n  selectAllAriaLabel: &#39;Select all positions&#39;,\n  selectRowAriaLabel: (row) =&gt; `Select ${row.original.symbol}`,\n});\n</code></pre>\n<pre><code class="language-html">&lt;nat-table-surface [state]=&quot;tableState()&quot; (rowSelectionChange)=&quot;onRowSelectionChange($event)&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; [enableRowSelection]=&quot;true&quot; accessibleName=&quot;Selectable positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p>Rows expose <code>aria-selected</code> while selection is enabled. The grid exposes <code>aria-multiselectable=&quot;true&quot;</code> only in multiple mode. Selection changes are announced through the table live region.</p>\n<h2>Final Review</h2>\n<p>Answer these questions before considering a table accessible:</p>\n<ul>\n<li>Can a screen-reader user identify the table without seeing the page?</li>\n<li>Are all column labels meaningful outside the visual layout?</li>\n<li>Do custom cells expose names and keyboard behavior through real controls?</li>\n<li>Do state rows communicate loading, empty, and error states through text?</li>\n<li>Are generated summaries, controls, and announcements localized?</li>\n<li>Do focus indicators, text, controls, pinned columns, and semantic tones meet WCAG AA contrast?</li>\n</ul>\n',
    headings: [
      {
        depth: 2,
        id: 'minimum-checklist'
      },
      {
        depth: 2,
        id: 'table-name'
      },
      {
        depth: 2,
        id: 'column-metadata'
      },
      {
        depth: 2,
        id: 'hidden-header-labels'
      },
      {
        depth: 2,
        id: 'table-specific-copy'
      },
      {
        depth: 2,
        id: 'localization-providers'
      },
      {
        depth: 2,
        id: 'state-rows'
      },
      {
        depth: 2,
        id: 'custom-interactive-cells'
      },
      {
        depth: 2,
        id: 'selection-accessibility'
      },
      {
        depth: 2,
        id: 'final-review'
      }
    ]
  },
  '/docs/column-layout.md': {
    html: '<h2>Layout Responsibilities</h2>\n<p>Column layout covers how columns present at runtime: pinning, ordering, sizing, visibility, and sticky headers. Keep column meaning and cell rendering in the Columns topic; use this topic for presentation state.</p>\n<h2>Pinning And Visibility</h2>\n<p>Pinning keeps important columns at a scroll boundary. Visibility lets users remove columns that are not useful for the current task. Both are state slices, so app-owned controls and companion controls should patch the same table state.</p>\n<h2>Reordering And Resizing</h2>\n<p>Reordering changes the rendered column order. It is disabled by default; set <code>[enableReordering]=&quot;true&quot;</code> on the surface before exposing drag/drop, header move menus, or keyboard column moves. Resizing changes width state. Both need keyboard support because pointer-only layout controls are not accessible enough for this table library.</p>\n<p>Use <code>columnSizingMode=&quot;fill&quot;</code> when columns should stretch to the available width. Use <code>columnSizingMode=&quot;fixed&quot;</code> when column widths should stay authoritative and the table may scroll horizontally.</p>\n<pre><code class="language-html">&lt;nat-table-surface [enableReordering]=&quot;true&quot; columnSizingMode=&quot;fixed&quot; columnResizeMode=&quot;onEnd&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n  &lt;nat-table-scroll-control /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p><code>columnResizeMode=&quot;onEnd&quot;</code> commits a pointer resize after release. Use <code>&quot;onChange&quot;</code> only when live resizing is important enough to update layout continuously during the drag.</p>\n<h2>Horizontal Scroll Controls</h2>\n<p><code>NatTableScrollControl</code> provides buttons, a range input, and position text for the table&#39;s horizontal scroll container. It is most useful with fixed sizing, pinned columns, or wide comparison tables where native horizontal scrollbars are easy to miss.</p>\n<pre><code class="language-html">&lt;nat-table-scroll-control [scrollStep]=&quot;320&quot; groupAriaLabel=&quot;Horizontal table scroll&quot; [accessibilityLabels]=&quot;scrollLabels&quot; /&gt;\n</code></pre>\n<pre><code class="language-ts">import type { NatTableAccessibilityScrollControlLabels } from &#39;ng-advanced-table/locale&#39;;\n\nreadonly scrollLabels = {\n  scrollLeftAriaLabel: &#39;Scroll positions left&#39;,\n  scrollRightAriaLabel: &#39;Scroll positions right&#39;,\n  scrollPositionAriaLabel: &#39;Horizontal position&#39;,\n  scrollPositionText: ({ percentageText }) =&gt; `${percentageText}% scrolled`,\n} satisfies NatTableAccessibilityScrollControlLabels;\n</code></pre>\n<p>Place the control inside the same <code>NatTableSurface</code> as the table. The control resolves the table scroll container through the surface controller and disables itself when there is no horizontal overflow. Keep <code>scrollStep</code> large enough to feel useful but smaller than the visible table width, so each button press preserves context.</p>\n<h2>Sticky Header</h2>\n<p>Use a sticky header when the table has enough rows that users lose column context while scrolling. Keep the vertical region height intentional so the sticky behavior is visible and predictable.</p>\n',
    headings: [
      {
        depth: 2,
        id: 'layout-responsibilities'
      },
      {
        depth: 2,
        id: 'pinning-and-visibility'
      },
      {
        depth: 2,
        id: 'reordering-and-resizing'
      },
      {
        depth: 2,
        id: 'horizontal-scroll-controls'
      },
      {
        depth: 2,
        id: 'sticky-header'
      }
    ]
  },
  '/docs/columns.md': {
    html: '<p>Columns are <code>ColumnDef&lt;TData&gt;[]</code> imported from <code>ng-advanced-table</code>. Put product-specific rendering in your column definitions and keep table-wide wiring in the surface.</p>\n<h2>Basic Column Shape</h2>\n<p>Use <code>accessorKey</code> for fields and <code>id</code> for display or computed columns. Provide <code>meta.label</code> for every column.</p>\n<pre><code class="language-ts">import { type ColumnDef } from &#39;ng-advanced-table&#39;;\n\ninterface PositionRow {\n  id: string;\n  symbol: string;\n  company: string;\n  price: number;\n  changePercent: number;\n}\n\nreadonly columns: ColumnDef&lt;PositionRow&gt;[] = [\n  {\n    accessorKey: &#39;symbol&#39;,\n    header: &#39;Symbol&#39;,\n    meta: { label: &#39;Symbol&#39;, rowHeader: true },\n  },\n  {\n    accessorKey: &#39;company&#39;,\n    header: &#39;Company&#39;,\n    meta: { label: &#39;Company&#39;, cellMaxLines: 2 },\n  },\n  {\n    accessorKey: &#39;price&#39;,\n    header: &#39;Last&#39;,\n    meta: { label: &#39;Last&#39;, align: &#39;end&#39; },\n    cell: (context) =&gt; currency.format(context.getValue&lt;number&gt;()),\n  },\n];\n</code></pre>\n<h2>Column Metadata</h2>\n<p><code>NatTableColumnMeta</code> adds table-specific behavior to <code>columnDef.meta</code>.</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Use it for</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>label</code></td>\n<td>Stable human-readable label for announcements and companion UI</td>\n</tr>\n<tr>\n<td><code>hiddenHeaderLabel</code></td>\n<td>Screen-reader-only header text for compact utility columns</td>\n</tr>\n<tr>\n<td><code>align</code></td>\n<td><code>&#39;start&#39;</code> or <code>&#39;end&#39;</code> header and body alignment</td>\n</tr>\n<tr>\n<td><code>rowHeader</code></td>\n<td>Marks body cells in the column as row headers</td>\n</tr>\n<tr>\n<td><code>cellTone</code></td>\n<td>Semantic tone class for positive, negative, neutral, or warning cells</td>\n</tr>\n<tr>\n<td><code>cellHeight</code></td>\n<td>Fixed body-cell height for this column</td>\n</tr>\n<tr>\n<td><code>cellMaxLines</code></td>\n<td>Body-cell line clamp; defaults to <code>2</code>, use <code>Infinity</code> to disable</td>\n</tr>\n<tr>\n<td><code>headerSize</code></td>\n<td>Header-only width</td>\n</tr>\n<tr>\n<td><code>headerMinSize</code></td>\n<td>Header-only minimum width</td>\n</tr>\n<tr>\n<td><code>headerMaxSize</code></td>\n<td>Header-only maximum width</td>\n</tr>\n<tr>\n<td><code>export</code></td>\n<td>Export participation, header, and value mapping</td>\n</tr>\n</tbody></table>\n<pre><code class="language-ts">import type { NatTableColumnMeta } from &#39;ng-advanced-table&#39;;\n\ntype PositionColumnMeta = NatTableColumnMeta&lt;PositionRow&gt;;\n\nconst priceMeta: PositionColumnMeta = {\n  label: &#39;Last&#39;,\n  align: &#39;end&#39;,\n  cellTone: (context) =&gt; {\n    const change = context.row.original.changePercent;\n\n    if (change &gt; 0) return &#39;positive&#39;;\n    if (change &lt; 0) return &#39;negative&#39;;\n\n    return &#39;neutral&#39;;\n  }\n};\n</code></pre>\n<h2>Sizing</h2>\n<p>Use <code>size</code>, <code>minSize</code>, and <code>maxSize</code> for body-cell sizing. Header-only sizing lives in metadata.</p>\n<pre><code class="language-ts">readonly columns: ColumnDef&lt;PositionRow&gt;[] = [\n  {\n    accessorKey: &#39;symbol&#39;,\n    header: &#39;Symbol&#39;,\n    size: 112,\n    minSize: 96,\n    enableResizing: true,\n    meta: {\n      label: &#39;Symbol&#39;,\n      rowHeader: true,\n      headerSize: 132,\n    },\n  },\n  {\n    accessorKey: &#39;company&#39;,\n    header: &#39;Company&#39;,\n    minSize: 180,\n    maxSize: 360,\n    enableResizing: true,\n    meta: {\n      label: &#39;Company&#39;,\n      cellHeight: 64,\n      cellMaxLines: 2,\n    },\n  },\n];\n</code></pre>\n<p>Configure the table width model on the surface.</p>\n<pre><code class="language-html">&lt;nat-table-surface columnSizingMode=&quot;fixed&quot; columnResizeMode=&quot;onEnd&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n  &lt;nat-table-scroll-control /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p><code>columnSizingMode=&quot;fill&quot;</code> stretches columns to fill the container. <code>columnSizingMode=&quot;fixed&quot;</code> treats widths as authoritative and lets the table scroll horizontally. <code>columnResizeMode=&quot;onEnd&quot;</code> commits after pointer release; <code>&quot;onChange&quot;</code> updates during the drag.</p>\n<h2>Pinning And Reordering</h2>\n<p>Pinning is enabled where the column allows it. Reordering is disabled by default; enable it on the surface when a table should expose drag/drop, keyboard column moves, or companion move actions. Reordering stays inside the current pinning zone: left, center, or right.</p>\n<pre><code class="language-ts">readonly initialState: Partial&lt;NatTableUserState&gt; = {\n  columnPinning: {\n    left: [&#39;symbol&#39;],\n    right: [&#39;actions&#39;],\n  },\n  columnOrder: [&#39;symbol&#39;, &#39;company&#39;, &#39;price&#39;, &#39;changePercent&#39;, &#39;actions&#39;],\n};\n</code></pre>\n<pre><code class="language-ts">readonly columns = withNatTableHeaderActions(baseColumns, {\n  enableColumnReorderActions: true,\n});\n</code></pre>\n<p>The header actions helper adds sort buttons when a column can sort, pin menu items when the column can pin, and move menu items when <code>enableColumnReorderActions</code> is enabled. It also accepts custom sort indicator content through <code>sortIndicator</code>, so custom sort icons should compose through the helper rather than through extra header rows.</p>\n<p>Enable table-owned reordering on the surface for tables that should expose move affordances.</p>\n<pre><code class="language-html">&lt;nat-table-surface [enableReordering]=&quot;true&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<h2>Custom Cell Components</h2>\n<p>Use <code>flexRenderComponent(...)</code> when a cell should render an Angular component. Keep data loading, mutations, and dialogs in the container; the cell component should emit intent.</p>\n<pre><code class="language-ts">import { Component, input, output } from &#39;@angular/core&#39;;\nimport { GridCellWidget } from &#39;@angular/aria/grid&#39;;\nimport { flexRenderComponent, type ColumnDef } from &#39;ng-advanced-table&#39;;\n\n@Component({\n  selector: &#39;app-position-actions-cell&#39;,\n  imports: [GridCellWidget],\n  template: `\n    &lt;button\n      type=&quot;button&quot;\n      ngGridCellWidget\n      [attr.aria-label]=&quot;&#39;Open actions for &#39; + symbol()&quot;\n      (click)=&quot;opened.emit()&quot;\n    &gt;\n      Actions\n    &lt;/button&gt;\n  `,\n})\nexport class PositionActionsCell {\n  readonly symbol = input.required&lt;string&gt;();\n  readonly opened = output&lt;void&gt;();\n}\n\nreadonly columns: ColumnDef&lt;PositionRow&gt;[] = [\n  {\n    accessorKey: &#39;symbol&#39;,\n    header: &#39;Symbol&#39;,\n    meta: { label: &#39;Symbol&#39;, rowHeader: true },\n  },\n  {\n    id: &#39;actions&#39;,\n    header: &#39;Actions&#39;,\n    enableSorting: false,\n    enableGlobalFilter: false,\n    enableHiding: false,\n    meta: {\n      label: &#39;Actions&#39;,\n      hiddenHeaderLabel: &#39;Row actions&#39;,\n      align: &#39;end&#39;,\n      cellMaxLines: Infinity,\n    },\n    cell: (context) =&gt;\n      flexRenderComponent(PositionActionsCell, {\n        inputs: { symbol: context.row.original.symbol },\n        outputs: { opened: () =&gt; this.openActions(context.row.original.id) },\n      }),\n  },\n];\n</code></pre>\n<p>Put <code>ngGridCellWidget</code> on the real focusable element. If a design-system component renders the button internally, apply the widget marker inside that component.</p>\n<h2>Row Activation</h2>\n<p>Use <code>(rowActivate)</code> for row-level navigation or primary actions. It fires on a primary click or <code>Enter</code> / <code>Space</code>, but ignores activations that start inside interactive descendants.</p>\n<pre><code class="language-html">&lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; (rowActivate)=&quot;openPosition($event.rowData.id)&quot; /&gt;\n</code></pre>\n<pre><code class="language-ts">protected openPosition(positionId: string): void {\n  this.router.navigate([&#39;/positions&#39;, positionId]);\n}\n</code></pre>\n<p>Keep cell-level buttons, links, menus, and inputs independent. They should emit their own outputs and should not depend on row activation.</p>\n<h2>Export Metadata</h2>\n<p>Accessor columns export by default. Display columns opt out unless explicitly enabled. Use <code>meta.export</code> when exported data should differ from rendered text.</p>\n<pre><code class="language-ts">{\n  accessorKey: &#39;price&#39;,\n  header: &#39;Last&#39;,\n  meta: {\n    label: &#39;Last&#39;,\n    align: &#39;end&#39;,\n    export: {\n      header: &#39;Last price&#39;,\n      value: ({ value }) =&gt; Number(value),\n    },\n  },\n  cell: (context) =&gt; currency.format(context.getValue&lt;number&gt;()),\n}\n</code></pre>\n<p>Disable export for columns that are useful only on screen.</p>\n<pre><code class="language-ts">{\n  id: &#39;actions&#39;,\n  header: &#39;Actions&#39;,\n  enableSorting: false,\n  enableGlobalFilter: false,\n  meta: {\n    label: &#39;Actions&#39;,\n    export: { enabled: false },\n  },\n  cell: (context) =&gt; renderActions(context.row.original),\n}\n</code></pre>\n<h2>Common Mistakes</h2>\n<ul>\n<li>Do not omit <code>meta.label</code> because the visible <code>header</code> happens to be a string today.</li>\n<li>Do not rely on namespaced positional fallback ids for interactive tables.</li>\n<li>Do not make clickable <code>&lt;div&gt;</code> or <code>&lt;span&gt;</code> content inside cells; use real controls.</li>\n<li>Do not add fake header rows to customize sorting; use <code>withNatTableHeaderActions(..., { sortIndicator })</code>.</li>\n<li>Do not enable resizing on every column automatically. Choose the columns where resizing is useful.</li>\n<li>Do not move workflow state such as filters, dialogs, or mutations into cell components.</li>\n</ul>\n',
    headings: [
      {
        depth: 2,
        id: 'basic-column-shape'
      },
      {
        depth: 2,
        id: 'column-metadata'
      },
      {
        depth: 2,
        id: 'sizing'
      },
      {
        depth: 2,
        id: 'pinning-and-reordering'
      },
      {
        depth: 2,
        id: 'custom-cell-components'
      },
      {
        depth: 2,
        id: 'row-activation'
      },
      {
        depth: 2,
        id: 'export-metadata'
      },
      {
        depth: 2,
        id: 'common-mistakes'
      }
    ]
  },
  '/docs/composition.md': {
    html: '<p>Composition is the main design point of the table entry points. <code>ng-advanced-table</code> renders the grid and exposes a controller. <code>ng-advanced-table/components</code> provides optional companion controls that read and patch that controller. Your app owns domain workflows such as search copy, filter menus, row actions, bulk actions, dialogs, and server requests.</p>\n<h2>Entry Point Responsibilities</h2>\n<table>\n<thead>\n<tr>\n<th>Entry point</th>\n<th>Responsibility</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>ng-advanced-table</code></td>\n<td>Core table, keyboard grid behavior, TanStack integration, state rows, row activation, accessibility announcements</td>\n</tr>\n<tr>\n<td><code>ng-advanced-table/components</code></td>\n<td>Surface, pagination, column visibility, scroll controls, toolbar, header actions, selection column, export directive</td>\n</tr>\n<tr>\n<td><code>ng-advanced-table/render-metrics</code></td>\n<td>Optional render-metrics store, filter, panel, and synthetic metrics column</td>\n</tr>\n<tr>\n<td><code>ng-advanced-table/locale</code></td>\n<td>Built-in locale dictionaries and provider helpers for core accessibility, controls, and render-metrics copy</td>\n</tr>\n<tr>\n<td>Your app</td>\n<td>Search inputs, domain filters, row menus, bulk actions, fetch/retry flows, dialogs, routing, permissions</td>\n</tr>\n</tbody></table>\n<p>Keeping this boundary clear makes the reusable packages stable while still letting product-specific workflows feel native to your application.</p>\n<h2>Surface And Controller</h2>\n<p>Most companion controls should live inside <code>NatTableSurface</code>. The surface creates the scoped <code>NatTableService</code>, passes state inputs to the table, receives table state changes, and lets controls patch the same controller.</p>\n<pre><code class="language-html">&lt;nat-table-surface [initialState]=&quot;initialState&quot;&gt;\n  &lt;nat-table-pagination [pageSizeOptions]=&quot;[25, 50, 100]&quot; /&gt;\n\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n\n  &lt;nat-table-scroll-control /&gt;\n  &lt;nat-table-column-visibility /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p>The table registers itself as the controller for controls in that surface. In unusual layouts, controls that accept <code>for</code> can target an exported table instance directly.</p>\n<pre><code class="language-html">&lt;nat-table #grid=&quot;natTable&quot; [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n\n&lt;nat-table-toolbar [for]=&quot;grid&quot; accessibleName=&quot;Detached table actions&quot;&gt;\n  &lt;button type=&quot;button&quot; natToolbarItem natTableExport [for]=&quot;grid&quot;&gt;Export&lt;/button&gt;\n&lt;/nat-table-toolbar&gt;\n</code></pre>\n<p>Prefer the scoped surface for normal pages. Use direct controller binding only when the layout requires controls outside the surface.</p>\n<h2>Stock Controls</h2>\n<table>\n<thead>\n<tr>\n<th>Control</th>\n<th>Use it for</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>NatTableSurface</code></td>\n<td>Controller scope, companion-control wiring, and state binding (styling is opt-in — see Theming)</td>\n</tr>\n<tr>\n<td><code>NatTableColumnVisibility</code></td>\n<td>Hide and show hideable columns</td>\n</tr>\n<tr>\n<td><code>NatTablePageSize</code></td>\n<td>Page-size chips</td>\n</tr>\n<tr>\n<td><code>NatTablePager</code></td>\n<td>Previous and next page controls</td>\n</tr>\n<tr>\n<td><code>NatTablePagination</code></td>\n<td>Combined page-size and pager toolbar</td>\n</tr>\n<tr>\n<td><code>NatTableScrollControl</code></td>\n<td>Horizontal scroll buttons and range control</td>\n</tr>\n<tr>\n<td><code>NatTableToolbar</code></td>\n<td>Roving-keyboard toolbar shell for table actions</td>\n</tr>\n<tr>\n<td><code>NatToolbarGroup</code>, <code>natToolbarItem</code></td>\n<td>Toolbar grouping and item registration</td>\n</tr>\n<tr>\n<td><code>withNatTableHeaderActions(...)</code></td>\n<td>Header sort buttons plus pin and move menus</td>\n</tr>\n<tr>\n<td><code>withNatTableSelectionColumn(...)</code></td>\n<td>Select-all and per-row checkbox column</td>\n</tr>\n<tr>\n<td><code>NatTableExport</code></td>\n<td>CSV export or app-provided export operation</td>\n</tr>\n</tbody></table>\n<h2>Header Actions</h2>\n<p>Wrap column definitions with <code>withNatTableHeaderActions(...)</code> when you want shared header controls for sorting, pinning, or menu-based reordering. Menu-based reordering still requires <code>[enableReordering]=&quot;true&quot;</code> on the table surface.</p>\n<pre><code class="language-html">&lt;nat-table-surface [enableReordering]=&quot;true&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<pre><code class="language-ts">import { withNatTableHeaderActions } from &#39;ng-advanced-table/components&#39;;\n\nreadonly columns = withNatTableHeaderActions&lt;PositionRow&gt;(\n  [\n    {\n      accessorKey: &#39;symbol&#39;,\n      header: &#39;Symbol&#39;,\n      enablePinning: true,\n      meta: { label: &#39;Symbol&#39;, rowHeader: true },\n    },\n    {\n      accessorKey: &#39;price&#39;,\n      header: &#39;Price&#39;,\n      enablePinning: true,\n      meta: { label: &#39;Price&#39;, align: &#39;end&#39; },\n    },\n  ],\n  {\n    enableColumnReorderActions: true,\n  },\n);\n</code></pre>\n<p>The helper preserves original header content and can be applied repeatedly. It is the intended composition point for header controls: keep the real header in <code>column.header</code>, add <code>meta.label</code>, then wrap the final column array. Do not add a second styled header row or replace the table header DOM to customize sort icons.</p>\n<p>Use <code>sortIndicator</code> when the design needs custom sort icon, badge, or text content while keeping the bundled sort behavior and labels.</p>\n<pre><code class="language-ts">import { Component, input } from &#39;@angular/core&#39;;\nimport { flexRenderComponent, type NatTableSortIndicatorContext } from &#39;ng-advanced-table&#39;;\nimport { withNatTableHeaderActions } from &#39;ng-advanced-table/components&#39;;\n\n@Component({\n  selector: &#39;app-sort-indicator&#39;,\n  template: `\n    &lt;span aria-hidden=&quot;true&quot; [attr.data-sort-state]=&quot;context().sortState || &#39;none&#39;&quot;&gt;\n      {{ context().sortState === &#39;asc&#39; ? &#39;Asc&#39; : context().sortState === &#39;desc&#39; ? &#39;Desc&#39; : &#39;Sort&#39; }}\n    &lt;/span&gt;\n  `,\n})\nexport class SortIndicator {\n  readonly context = input.required&lt;NatTableSortIndicatorContext&gt;();\n}\n\nreadonly columns = withNatTableHeaderActions(baseColumns, {\n  sortIndicator: (context) =&gt;\n    flexRenderComponent(SortIndicator, {\n      inputs: { context },\n    }),\n});\n</code></pre>\n<p>The custom indicator is visual only. The generated sort button still owns the click handler, keyboard behavior, accessible name, multi-sort state, and <code>aria-sort</code>.</p>\n<p>For utility columns, use <code>hiddenHeaderLabel</code> to keep a screen-reader label while hiding redundant visible header text.</p>\n<pre><code class="language-ts">{\n  id: &#39;actions&#39;,\n  header: &#39;Actions&#39;,\n  enableSorting: false,\n  enableGlobalFilter: false,\n  enableHiding: false,\n  meta: {\n    label: &#39;Actions&#39;,\n    hiddenHeaderLabel: &#39;Row actions&#39;,\n    headerActions: false,\n  },\n  cell: (context) =&gt; renderActions(context.row.original),\n}\n</code></pre>\n<p>Set <code>column.meta.headerActions</code> to <code>false</code> when a column should not get the wrapper, or pass a per-column object to override helper-level options for that column.</p>\n<h2>Toolbar Composition</h2>\n<p>Use <code>&lt;nat-table-toolbar&gt;</code> for generic command rows. Projected interactive controls that participate in toolbar keyboard navigation must use <code>natToolbarItem</code> or <code>NatToolbarGroup</code>.</p>\n<pre><code class="language-html">&lt;nat-table-toolbar accessibleName=&quot;Positions toolbar&quot;&gt;\n  &lt;button type=&quot;button&quot; natToolbarItem natToolbarItemPosition=&quot;start&quot;&gt;Refresh&lt;/button&gt;\n\n  &lt;div natToolbarGroup=&quot;end&quot; accessibleName=&quot;Density&quot;&gt;\n    &lt;button type=&quot;button&quot; natToolbarItem&gt;Compact&lt;/button&gt;\n    &lt;button type=&quot;button&quot; natToolbarItem&gt;Comfortable&lt;/button&gt;\n  &lt;/div&gt;\n\n  &lt;button type=&quot;button&quot; natToolbarItem natToolbarItemPosition=&quot;end&quot; natTableExport&gt;Export&lt;/button&gt;\n&lt;/nat-table-toolbar&gt;\n</code></pre>\n<p>Keep DOM order aligned with screen-reader and roving-keyboard order. Use start, center, and end positions for visual placement, but do not reorder the DOM in a way that makes keyboard navigation surprising.</p>\n<h2>Consumer-Owned Search</h2>\n<p>Global search is intentionally not a packaged UI primitive. Build a search component in your app, inject <code>NatTableService</code>, register search while the component is alive, and patch the table <code>globalFilter</code> state.</p>\n<pre><code class="language-ts">import { Component, DestroyRef, computed, inject, input } from &#39;@angular/core&#39;;\nimport { NatTableService, type PaginationState, type RowData } from &#39;ng-advanced-table&#39;;\nimport { NatToolbarItem } from &#39;ng-advanced-table/components&#39;;\n\n@Component({\n  selector: &#39;app-table-search&#39;,\n  imports: [NatToolbarItem],\n  template: `\n    &lt;input\n      type=&quot;search&quot;\n      natToolbarItem\n      [attr.aria-label]=&quot;label()&quot;\n      [value]=&quot;value()&quot;\n      [placeholder]=&quot;placeholder()&quot;\n      (input)=&quot;onInput($event)&quot; /&gt;\n  `\n})\nexport class TableSearch&lt;TData extends RowData = RowData&gt; {\n  readonly label = input(&#39;Search table&#39;);\n  readonly placeholder = input(&#39;Type to search...&#39;);\n\n  private readonly tableService = inject&lt;NatTableService&lt;TData&gt;&gt;(NatTableService);\n  private readonly destroyRef = inject(DestroyRef);\n\n  protected readonly controller = computed(() =&gt; this.tableService.controller());\n  protected readonly value = computed(() =&gt; this.controller()?.table.getState().globalFilter ?? &#39;&#39;);\n\n  constructor() {\n    this.tableService.registerSearch();\n    this.destroyRef.onDestroy(() =&gt; this.tableService.unregisterSearch());\n  }\n\n  protected onInput(event: Event): void {\n    const target = event.target;\n\n    if (!(target instanceof HTMLInputElement) || target.value === this.value()) {\n      return;\n    }\n\n    this.controller()?.patchState({\n      globalFilter: target.value,\n      pagination: (current: PaginationState) =&gt; ({ ...current, pageIndex: 0 })\n    });\n  }\n}\n</code></pre>\n<p>Then place it in a toolbar or beside the table.</p>\n<pre><code class="language-html">&lt;nat-table-surface&gt;\n  &lt;nat-table-toolbar accessibleName=&quot;Table actions&quot;&gt;\n    &lt;app-table-search label=&quot;Search by symbol, company, or desk&quot; /&gt;\n    &lt;button type=&quot;button&quot; natToolbarItem natTableExport exportFileName=&quot;positions&quot;&gt;Export&lt;/button&gt;\n  &lt;/nat-table-toolbar&gt;\n\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p>The app owns the input label, placeholder, debounce policy, URL synchronization, analytics, and server requests.</p>\n<h2>Domain Filters</h2>\n<p>Domain filters should usually be app-owned because their labels, options, counts, and fetch behavior are product-specific. Patch <code>columnFilters</code> when the filter is table-local, or filter the rows before passing them to the table when the table should not own the filter state.</p>\n<pre><code class="language-ts">protected toggleStatus(status: PositionStatus): void {\n  const selected = new Set(this.selectedStatuses());\n\n  if (selected.has(status)) {\n    selected.delete(status);\n  } else {\n    selected.add(status);\n  }\n\n  this.tableState.update((state) =&gt; ({\n    ...state,\n    columnFilters: upsertColumnFilter(state.columnFilters ?? [], &#39;status&#39;, [...selected]),\n    pagination: { ...(state.pagination ?? { pageIndex: 0, pageSize: 25 }), pageIndex: 0 },\n  }));\n}\n</code></pre>\n<p>Keep the filter controls accessible as normal application controls. Use button groups, checkboxes, menus, or segmented controls according to the product interaction, then update table state from those controls.</p>\n<h2>Helper Order</h2>\n<p>When composing column helpers, apply helpers that add synthetic columns first, then wrap the final array with header actions.</p>\n<pre><code class="language-ts">readonly metricsStore = new NatTableRenderMetricsStore();\n\nreadonly columns = withNatTableHeaderActions(\n  withRenderMetricsColumn(\n    withNatTableSelectionColumn(baseColumns, {\n      label: &#39;Selection&#39;,\n      selectRowAriaLabel: (row) =&gt; `Select ${row.original.name}`,\n    }),\n    this.metricsStore,\n  ),\n  {\n    enableColumnReorderActions: true,\n  },\n);\n</code></pre>\n<p>This prevents utility columns from being wrapped more than intended and lets header actions see the final column list.</p>\n',
    headings: [
      {
        depth: 2,
        id: 'entry-point-responsibilities'
      },
      {
        depth: 2,
        id: 'surface-and-controller'
      },
      {
        depth: 2,
        id: 'stock-controls'
      },
      {
        depth: 2,
        id: 'header-actions'
      },
      {
        depth: 2,
        id: 'toolbar-composition'
      },
      {
        depth: 2,
        id: 'consumer-owned-search'
      },
      {
        depth: 2,
        id: 'domain-filters'
      },
      {
        depth: 2,
        id: 'helper-order'
      }
    ]
  },
  '/docs/data-lifecycle.md': {
    html: '<p><code>NatTable</code> owns how loading, empty, and error states appear inside the table body. Your app owns data fetching, retry handling, request cancellation, error classification, and Manual Data Handling.</p>\n<h2>State Model</h2>\n<p>Use <code>dataStatus</code> to tell the table whether rows are loading, errored, or available.</p>\n<table>\n<thead>\n<tr>\n<th><code>dataStatus</code></th>\n<th>Rows</th>\n<th>Rendered body state</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>&#39;loading&#39;</code></td>\n<td><code>[]</code></td>\n<td>Loading row</td>\n</tr>\n<tr>\n<td><code>&#39;loading&#39;</code></td>\n<td>existing rows</td>\n<td>Existing rows remain visible and the grid gets <code>aria-busy=&quot;true&quot;</code></td>\n</tr>\n<tr>\n<td><code>&#39;error&#39;</code></td>\n<td>any rows</td>\n<td>Error row</td>\n</tr>\n<tr>\n<td><code>&#39;success&#39;</code></td>\n<td><code>[]</code></td>\n<td>Empty row</td>\n</tr>\n<tr>\n<td><code>&#39;success&#39;</code></td>\n<td>rows</td>\n<td>Data rows</td>\n</tr>\n</tbody></table>\n<p>The default status is <code>&#39;success&#39;</code>.</p>\n<h2>Basic Fetching</h2>\n<p>Keep request state in the consuming component and pass it to the table.</p>\n<pre><code class="language-ts">import { Component, signal } from &#39;@angular/core&#39;;\nimport {\n  NAT_TABLE_DATA_STATUS,\n  NatTable,\n  NatTableEmptyTemplate,\n  NatTableErrorTemplate,\n  NatTableLoadingTemplate,\n  type NatTableDataStatus\n} from &#39;ng-advanced-table&#39;;\n\n@Component({\n  selector: &#39;app-positions-table&#39;,\n  imports: [NatTable, NatTableLoadingTemplate, NatTableEmptyTemplate, NatTableErrorTemplate],\n  templateUrl: &#39;./positions-table.html&#39;\n})\nexport class PositionsTable {\n  readonly rows = signal&lt;readonly PositionRow[]&gt;([]);\n  readonly status = signal&lt;NatTableDataStatus&gt;(NAT_TABLE_DATA_STATUS.loading);\n  readonly error = signal&lt;unknown&gt;(null);\n\n  load(): void {\n    this.status.set(NAT_TABLE_DATA_STATUS.loading);\n    this.error.set(null);\n\n    this.positionsApi.list().subscribe({\n      next: (rows) =&gt; {\n        this.rows.set(rows);\n        this.status.set(NAT_TABLE_DATA_STATUS.success);\n      },\n      error: (error: unknown) =&gt; {\n        this.error.set(error);\n        this.status.set(NAT_TABLE_DATA_STATUS.error);\n      }\n    });\n  }\n}\n</code></pre>\n<pre><code class="language-html">&lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; [dataStatus]=&quot;status()&quot; [error]=&quot;error()&quot; accessibleName=&quot;Open positions&quot;&gt;\n  &lt;ng-template natTableLoading&gt;\n    &lt;strong&gt;Loading positions&lt;/strong&gt;\n    &lt;span&gt;Fetching the latest open positions.&lt;/span&gt;\n  &lt;/ng-template&gt;\n\n  &lt;ng-template natTableEmpty let-filtered=&quot;filtered&quot;&gt;\n    &lt;strong&gt;No positions found&lt;/strong&gt;\n    &lt;span&gt; {{ filtered ? &#39;No rows match the active filters.&#39; : &#39;There are no open positions.&#39; }} &lt;/span&gt;\n  &lt;/ng-template&gt;\n\n  &lt;ng-template natTableError let-error&gt;\n    &lt;strong&gt;Positions unavailable&lt;/strong&gt;\n    &lt;span&gt;{{ formatError(error) }}&lt;/span&gt;\n    &lt;button type=&quot;button&quot; (click)=&quot;load()&quot;&gt;Retry&lt;/button&gt;\n  &lt;/ng-template&gt;\n&lt;/nat-table&gt;\n</code></pre>\n<p>Focusable controls inside state templates are managed by the table. Use normal buttons, links, and inputs.</p>\n<p>State template content is placed in a centered flex column that fills the visible table width, so projected rich content (illustrations, custom elements, block/flex layouts) lays out with a resolvable width — no consumer CSS gymnastics required. Reserve vertical space with <code>--nat-table-state-min-height</code> (defaults to <code>--nat-table-min-height</code>, else <code>0</code>) so the state row does not collapse when the table has no data.</p>\n<h2>Background Refresh</h2>\n<p>When refreshing existing data, keep the rows visible and set <code>dataStatus=&quot;loading&quot;</code>.</p>\n<pre><code class="language-ts">refresh(): void {\n  this.status.set(NAT_TABLE_DATA_STATUS.loading);\n\n  this.positionsApi.list().subscribe({\n    next: (rows) =&gt; {\n      this.rows.set(rows);\n      this.status.set(NAT_TABLE_DATA_STATUS.success);\n    },\n    error: (error: unknown) =&gt; {\n      this.error.set(error);\n      this.status.set(NAT_TABLE_DATA_STATUS.error);\n    },\n  });\n}\n</code></pre>\n<p>If <code>rows()</code> still contains data while the status is loading, the table keeps the data rows visible and marks the grid busy. This avoids a disruptive empty loading row for background refreshes.</p>\n<h2>Empty State</h2>\n<p>An empty row is derived from <code>dataStatus=&quot;success&quot;</code> plus no visible rows. Use the <code>filtered</code> template context to distinguish &quot;no data&quot; from &quot;filters removed all rows&quot;.</p>\n<pre><code class="language-html">&lt;ng-template natTableEmpty let-filtered=&quot;filtered&quot; let-totalRowsValue=&quot;totalRowsValue&quot;&gt;\n  @if (filtered) {\n  &lt;strong&gt;No matching positions&lt;/strong&gt;\n  &lt;span&gt;Clear search or filters to show {{ totalRowsValue }} total rows.&lt;/span&gt;\n  } @else {\n  &lt;strong&gt;No positions yet&lt;/strong&gt;\n  &lt;span&gt;Create a position to populate this table.&lt;/span&gt;\n  }\n&lt;/ng-template&gt;\n</code></pre>\n<h2>Error State</h2>\n<p>Pass the raw error through <code>[error]</code> only if the template or logging flow needs it. The visible message should be product copy, not raw exception text.</p>\n<pre><code class="language-ts">protected formatError(error: unknown): string {\n  if (error instanceof HttpErrorResponse &amp;&amp; error.status === 403) {\n    return &#39;You do not have access to this portfolio.&#39;;\n  }\n\n  return &#39;The request failed. Try again.&#39;;\n}\n</code></pre>\n<pre><code class="language-html">&lt;ng-template natTableError let-error&gt;\n  &lt;strong&gt;Portfolio unavailable&lt;/strong&gt;\n  &lt;span&gt;{{ formatError(error) }}&lt;/span&gt;\n  &lt;button type=&quot;button&quot; (click)=&quot;load()&quot;&gt;Retry&lt;/button&gt;\n&lt;/ng-template&gt;\n</code></pre>\n<p>Keep retry and error classification in the container. The table should not know about HTTP status codes, permissions, or business-specific recovery flows.</p>\n<h2>Manual Data Handling</h2>\n<p>Use manual modes when your app owns pagination, sorting, or filtering outside the table. The surface emits state; your component prepares rows and passes back the current page or row set.</p>\n<pre><code class="language-html">&lt;nat-table-surface\n  [mode]=&quot;{ pagination: &#39;manual&#39;, sorting: &#39;manual&#39;, filtering: &#39;manual&#39; }&quot;\n  [manualPageCount]=&quot;pageCount()&quot;\n  [state]=&quot;tableState()&quot;\n  (stateChange)=&quot;onTableStateChange($event)&quot;&gt;\n  &lt;nat-table-toolbar accessibleName=&quot;Positions toolbar&quot;&gt;\n    &lt;app-table-search label=&quot;Search positions&quot; /&gt;\n    &lt;button type=&quot;button&quot; natToolbarItem (click)=&quot;reload()&quot;&gt;Refresh&lt;/button&gt;\n  &lt;/nat-table-toolbar&gt;\n\n  &lt;nat-table-pagination [pageSizeOptions]=&quot;[25, 50, 100]&quot; /&gt;\n\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; [dataStatus]=&quot;status()&quot; [error]=&quot;error()&quot; accessibleName=&quot;Manual positions&quot;&gt;\n    &lt;ng-template natTableLoading&gt;Loading positions&lt;/ng-template&gt;\n    &lt;ng-template natTableEmpty&gt;No positions match the current request&lt;/ng-template&gt;\n    &lt;ng-template natTableError&gt;Positions could not be loaded&lt;/ng-template&gt;\n  &lt;/nat-table&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<pre><code class="language-ts">protected onTableStateChange(state: Partial&lt;NatTableUserState&gt;): void {\n  this.tableState.set(state);\n  this.loadServerRows(state);\n}\n\nprivate loadServerRows(state: Partial&lt;NatTableUserState&gt;): void {\n  this.status.set(NAT_TABLE_DATA_STATUS.loading);\n  this.error.set(null);\n  const pagination = state.pagination ?? { pageIndex: 0, pageSize: 25 };\n\n  this.positionsApi\n    .list({\n      pageIndex: pagination.pageIndex,\n      pageSize: pagination.pageSize,\n      sorting: state.sorting ?? [],\n      globalFilter: state.globalFilter ?? &#39;&#39;,\n      columnFilters: state.columnFilters ?? [],\n    })\n    .subscribe({\n      next: (result) =&gt; {\n        this.rows.set(result.rows);\n        this.pageCount.set(result.pageCount);\n        this.status.set(NAT_TABLE_DATA_STATUS.success);\n      },\n      error: (error: unknown) =&gt; {\n        this.error.set(error);\n        this.status.set(NAT_TABLE_DATA_STATUS.error);\n      },\n    });\n}\n</code></pre>\n<p>For production request flows, cancel or ignore stale requests when a newer table state arrives. Keep that policy in the container or data service.</p>\n<h2>Custom Filter Functions</h2>\n<p>For client-side tables, define column filter functions on the relevant columns and update <code>columnFilters</code> from app-owned controls.</p>\n<pre><code class="language-ts">import type { ColumnDef, FilterFn } from &#39;ng-advanced-table&#39;;\n\nconst statusFilter: FilterFn&lt;PositionRow&gt; = (row, columnId, filterValue) =&gt; {\n  const selected = (filterValue ?? []) as PositionStatus[];\n\n  if (!selected.length) {\n    return true;\n  }\n\n  return selected.includes(row.getValue(columnId) as PositionStatus);\n};\n\nreadonly columns: ColumnDef&lt;PositionRow&gt;[] = [\n  {\n    accessorKey: &#39;status&#39;,\n    header: &#39;Status&#39;,\n    filterFn: statusFilter,\n    meta: { label: &#39;Status&#39; },\n  },\n];\n</code></pre>\n<pre><code class="language-ts">protected updateStatusFilter(statuses: readonly PositionStatus[]): void {\n  this.tableState.update((state) =&gt; ({\n    ...state,\n    columnFilters: upsertColumnFilter(state.columnFilters ?? [], &#39;status&#39;, statuses),\n    pagination: { ...(state.pagination ?? { pageIndex: 0, pageSize: 25 }), pageIndex: 0 },\n  }));\n}\n</code></pre>\n<h2>Practical Rules</h2>\n<ul>\n<li>Keep <code>dataStatus</code> as the table-owned switch for body state rows.</li>\n<li>Keep fetching, retries, stale request handling, and error classification outside the table.</li>\n<li>Use loading with existing rows for background refreshes.</li>\n<li>Use <code>natTableEmpty</code> for both no-data and filtered-empty messages.</li>\n<li>Reset <code>pageIndex</code> when a user changes global search or column filters.</li>\n<li>Use rows with stable string or number <code>id</code> values, or provide <code>getRowId</code> when identity is custom.</li>\n</ul>\n',
    headings: [
      {
        depth: 2,
        id: 'state-model'
      },
      {
        depth: 2,
        id: 'basic-fetching'
      },
      {
        depth: 2,
        id: 'background-refresh'
      },
      {
        depth: 2,
        id: 'empty-state'
      },
      {
        depth: 2,
        id: 'error-state'
      },
      {
        depth: 2,
        id: 'manual-data-handling'
      },
      {
        depth: 2,
        id: 'custom-filter-functions'
      },
      {
        depth: 2,
        id: 'practical-rules'
      }
    ]
  },
  '/docs/export.md': {
    html: '<h2>Export Button</h2>\n<p>Table Data Export is an optional Table Action from <code>ng-advanced-table/components</code>. Add <code>natTableExport</code> to an interactive host element and place it wherever the command belongs.</p>\n<pre><code class="language-html">&lt;button type=&quot;button&quot; natToolbarItem natTableExport exportFileName=&quot;services&quot;&gt;Export CSV&lt;/button&gt;\n</code></pre>\n<p>The directive resolves the table controller from the surrounding surface. It sets busy/disabled state while exporting and downloads a CSV by default.</p>\n<h2>Export Scope</h2>\n<p>The default CSV export uses all rows currently held by the client table, visible exportable leaf columns in current column order, and <code>meta.export</code> overrides for headers, inclusion, and values.</p>\n<p>It does not export all records that may exist in an external data source. Use a custom Export Handler when export scope must include remote records, selected rows, audit logging, or a server-generated file.</p>\n<h2>Column Export Metadata</h2>\n<p>Accessor columns export by default. Display columns opt out unless <code>meta.export.enabled</code> is set. Use <code>meta.export</code> when exported data should differ from rendered cell text.</p>\n<h2>Custom Export Handler</h2>\n<p>Provide a per-button handler when one export action needs custom behavior, or <code>provideNatTableExport(...)</code> when every export button should use the same application service.</p>\n<p>Handlers can call <code>context.getData()</code> for normalized export data or <code>context.exportCsv()</code> to delegate back to the built-in CSV handler.</p>\n',
    headings: [
      {
        depth: 2,
        id: 'export-button'
      },
      {
        depth: 2,
        id: 'export-scope'
      },
      {
        depth: 2,
        id: 'column-export-metadata'
      },
      {
        depth: 2,
        id: 'custom-export-handler'
      }
    ]
  },
  '/docs/filtering-search.md': {
    html: '<h2>Search Is App-Owned</h2>\n<p>Search copy, matching rules, filter menus, and request timing are product concerns. Use consumer-owned controls that patch table state rather than expecting the library to ship a generic search box for every workflow.</p>\n<h2>Global Filters</h2>\n<p>Use <code>globalFilter</code> when one input should narrow rows across the table. Reset <code>pagination.pageIndex</code> when a filter changes so users do not stay on an empty later page.</p>\n<pre><code class="language-ts">this.tableState.update((state) =&gt; ({\n  ...state,\n  globalFilter: value,\n  pagination: { ...(state.pagination ?? { pageIndex: 0, pageSize: 25 }), pageIndex: 0 }\n}));\n</code></pre>\n<h2>Column Filters</h2>\n<p>Use <code>columnFilters</code> when a control targets a specific column. Define the column&#39;s filter function near the column definition so the matching rule stays close to the field it interprets.</p>\n<h2>Manual Data Handling</h2>\n<p>When filtering happens outside the table, set filtering to manual mode and pass already-filtered rows into <code>NatTable</code>. The external pipeline may be a backend, local cache, worker, or store.</p>\n<pre><code class="language-html">&lt;nat-table-surface [mode]=&quot;{ filtering: &#39;manual&#39;, pagination: &#39;manual&#39; }&quot; [(state)]=&quot;tableState&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Filtered results&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n',
    headings: [
      {
        depth: 2,
        id: 'search-is-app-owned'
      },
      {
        depth: 2,
        id: 'global-filters'
      },
      {
        depth: 2,
        id: 'column-filters'
      },
      {
        depth: 2,
        id: 'manual-data-handling'
      }
    ]
  },
  '/docs/keyboard-interaction.md': {
    html: '<h2>Grid Navigation</h2>\n<p>The table uses an ARIA grid keyboard model. Users move between cells with arrow keys and can enter interactive cell content without losing the table navigation context.</p>\n<h2>Interactive Cells</h2>\n<p>Press Enter on a focused grid cell to move focus into the first interactive control. Tab and Shift+Tab move through controls inside the grid, and Escape returns focus to the cell.</p>\n<p>Cells whose entire content is one arrow-safe control can delegate directly to that control. Inputs, selects, comboboxes, and other arrow-consuming controls stay on the Enter-to-interact model.</p>\n<h2>Column Shortcuts</h2>\n<p>Column reordering exposes keyboard paths when <code>[enableReordering]=&quot;true&quot;</code> is set on the table surface. Resizing exposes keyboard paths for resizable columns. Keep visible instructions and configured shortcuts aligned with <code>aria-keyshortcuts</code> when you customize keybindings.</p>\n<h2>Custom Keybindings</h2>\n<p>Use <code>[keybindings]</code> on the table surface for per-table overrides, or <code>NAT_TABLE_KEYBINDINGS</code> for app-wide defaults. The table warns in development when configured shortcuts conflict.</p>\n<p>Use <code>NatTableHotkeyA11y</code> when custom controls need shortcut names in their accessible names and <code>aria-keyshortcuts</code>.</p>\n',
    headings: [
      {
        depth: 2,
        id: 'grid-navigation'
      },
      {
        depth: 2,
        id: 'interactive-cells'
      },
      {
        depth: 2,
        id: 'column-shortcuts'
      },
      {
        depth: 2,
        id: 'custom-keybindings'
      }
    ]
  },
  '/docs/localization.md': {
    html: '<h2>Locale Providers</h2>\n<p>Generated table copy resolves from the <code>ng-advanced-table/locale</code> entry point. Add only the providers for entry points you use.</p>\n<pre><code class="language-ts">provideNatTableLocales(...);\nprovideNatTableControlsLocales(...);\nprovideNatTableRenderMetricsLocales(...);\n</code></pre>\n<p>Core table copy, companion UI controls, and render-metrics utilities have separate locale sections so applications can adopt them independently.</p>\n<p>Provider helpers also accept a factory callback when copy comes from an injectable translation service. The callback runs inside Angular dependency injection, and its return value is merged with parent providers the same way static config is merged.</p>\n<pre><code class="language-ts">import { inject } from &#39;@angular/core&#39;;\nimport { provideNatTableIntl, provideNatTableControlsIntl, provideNatTableRenderMetricsIntl } from &#39;ng-advanced-table/locale&#39;;\n\nproviders: [\n  provideNatTableIntl(() =&gt; {\n    const translate = inject(TranslateService);\n\n    return {\n      accessibilityText: {\n        emptyState: translate.instant(&#39;table.empty&#39;),\n        loadingState: translate.instant(&#39;table.loading&#39;)\n      }\n    };\n  }),\n  provideNatTableControlsIntl(() =&gt; {\n    const translate = inject(TranslateService);\n\n    return {\n      headerActions: {\n        accessibilityLabels: {\n          sortButton: ({ label }) =&gt; translate.instant(&#39;table.sortButton&#39;, { label })\n        }\n      }\n    };\n  }),\n  provideNatTableRenderMetricsIntl(() =&gt; {\n    const translate = inject(TranslateService);\n\n    return {\n      renderMetrics: {\n        panel: {\n          ariaLabel: translate.instant(&#39;table.renderMetrics.panel&#39;)\n        }\n      }\n    };\n  })\n];\n</code></pre>\n<p>Return a single locale dictionary to override English defaults, or return <code>{ locales: { ... } }</code> when registering several locale ids.</p>\n<h2>Runtime Locale Changes</h2>\n<p>Provider factory callbacks are resolved once per injector, so calls such as <code>translate.instant(...)</code> capture the copy for the active language at provider resolution time.\nPass the active locale to <code>NatTableSurface</code> when the locale can change at runtime. Rebuild translated column definitions from the same translation source so headers, metadata labels, and generated helper text stay aligned.</p>\n<h2>Accessible Names</h2>\n<p>When visible text changes, keep visible words inside accessible names. This matters for toolbar controls, selection checkboxes, export buttons, render-metrics controls, and custom icon-only buttons.</p>\n<h2>Column Labels</h2>\n<p><code>meta.label</code> is used by summaries, live announcements, companion controls, header actions, selection/export helpers, and custom sort indicators. Localize it whenever the column header is localized.</p>\n',
    headings: [
      {
        depth: 2,
        id: 'locale-providers'
      },
      {
        depth: 2,
        id: 'runtime-locale-changes'
      },
      {
        depth: 2,
        id: 'accessible-names'
      },
      {
        depth: 2,
        id: 'column-labels'
      }
    ]
  },
  '/docs/pagination.md': {
    html: '<h2>When To Use Pagination</h2>\n<p>Use pagination when the user needs a smaller visible working set or when data is naturally handled in pages. Prefer no pagination when the row count is small and scanning the whole table is faster.</p>\n<h2>Client Pagination</h2>\n<p><code>NatTablePagination</code> enables the pagination row model and patches the <code>pagination</code> state slice. Provide page-size options that match the density and workflow of the table.</p>\n<pre><code class="language-html">&lt;nat-table-surface [(state)]=&quot;tableState&quot;&gt;\n  &lt;nat-table-pagination [pageSizeOptions]=&quot;[25, 50, 100]&quot; /&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<h2>Split Page-Size And Pager Controls</h2>\n<p>Use <code>NatTablePagination</code> when the page-size chips and previous/next buttons should render as one toolbar. Use <code>NatTablePageSize</code> and <code>NatTablePager</code> separately when the layout needs them in different regions, such as page size near table filters and paging controls near the table footer.</p>\n<pre><code class="language-html">&lt;nat-table-surface [(state)]=&quot;tableState&quot;&gt;\n  &lt;div class=&quot;table-controls&quot;&gt;\n    &lt;nat-table-page-size [pageSizeOptions]=&quot;[25, 50, 100]&quot; groupAriaLabel=&quot;Rows per page&quot; /&gt;\n  &lt;/div&gt;\n\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n\n  &lt;nat-table-pager groupAriaLabel=&quot;Position pages&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p>Both split controls register pagination with the table surface, update the same <code>pagination</code> state slice, and use the active UI locale for generated labels. Pass <code>pageSizeOptions</code> to <code>NatTablePageSize</code>; pass pager label overrides to <code>NatTablePager</code> only when the default locale copy is not specific enough for the table.</p>\n<h2>Manual Pagination</h2>\n<p>Use manual pagination when your app prepares the visible rows outside the table. Pass <code>manualPageCount</code> so the pager knows how many pages exist in the external row pipeline.</p>\n<pre><code class="language-html">&lt;nat-table-surface [manualPageCount]=&quot;pageCount()&quot; [mode]=&quot;{ pagination: &#39;manual&#39; }&quot; [(state)]=&quot;tableState&quot;&gt;\n  &lt;nat-table-pagination [pageSizeOptions]=&quot;[25, 50, 100]&quot; /&gt;\n  &lt;nat-table [data]=&quot;pageRows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Paged results&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p>Manual pagination is often combined with manual filtering or sorting, but it is not limited to server requests.</p>\n',
    headings: [
      {
        depth: 2,
        id: 'when-to-use-pagination'
      },
      {
        depth: 2,
        id: 'client-pagination'
      },
      {
        depth: 2,
        id: 'split-page-size-and-pager-controls'
      },
      {
        depth: 2,
        id: 'manual-pagination'
      }
    ]
  },
  '/docs/quick-start.md': {
    html: '<p>Using an AI assistant? Start with the <code>/nat-best-practises</code> skill so generated code follows the table API, state model, accessibility rules, and styling contract.</p>\n<h2>Recommended Agent Setup</h2>\n<p>Install the table package and Angular companion peers:</p>\n<pre><code class="language-bash">pnpm add ng-advanced-table @angular/aria @angular/cdk\n# or: npm install ng-advanced-table @angular/aria @angular/cdk\n# or: yarn add ng-advanced-table @angular/aria @angular/cdk\n</code></pre>\n<p>Keep <code>@angular/core</code> and <code>@angular/common</code> in your Angular app dependencies.</p>\n<p>Install the agent skill:</p>\n<pre><code class="language-bash">npx skills add VaggelisKa/angular-advanced-table --skill nat-best-practises\n</code></pre>\n<p>Start by asking for the table contract:</p>\n<pre><code class="language-text">Use /nat-best-practises to build an Angular table for invoices with columns for number, customer, status, and total. Start with the row type, row id, columns, and accessibility checks.\n</code></pre>\n<p>Review that contract before generating code.</p>\n<h2>Manual Setup</h2>\n<p>If you are wiring the table by hand, start with <code>NatTable</code> inside <code>NatTableSurface</code>.</p>\n<h2>First Table</h2>\n<p>Create rows and columns, then render <code>NatTable</code> inside <code>NatTableSurface</code>. Give every table an <code>accessibleName</code> or <code>caption</code>.</p>\n<pre><code class="language-ts">import { Component } from &#39;@angular/core&#39;;\n\nimport { NatTable } from &#39;ng-advanced-table&#39;;\nimport { NatTableSurface } from &#39;ng-advanced-table/components&#39;;\n\ninterface PositionRow {\n  id: string;\n  symbol: string;\n  company: string;\n}\n\n@Component({\n  selector: &#39;app-positions-table&#39;,\n  imports: [NatTable, NatTableSurface],\n  template: `\n    &lt;nat-table-surface&gt;\n      &lt;nat-table [data]=&quot;rows&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n    &lt;/nat-table-surface&gt;\n  `\n})\nexport class PositionsTable {\n  readonly rows: readonly PositionRow[] = [\n    { id: &#39;pos-1&#39;, symbol: &#39;AAPL&#39;, company: &#39;Apple&#39; },\n    { id: &#39;pos-2&#39;, symbol: &#39;MSFT&#39;, company: &#39;Microsoft&#39; }\n  ];\n\n  readonly columns = [\n    {\n      accessorKey: &#39;symbol&#39;,\n      header: &#39;Symbol&#39;,\n      meta: { label: &#39;Symbol&#39;, rowHeader: true }\n    },\n    {\n      accessorKey: &#39;company&#39;,\n      header: &#39;Company&#39;,\n      meta: { label: &#39;Company&#39; }\n    }\n  ];\n}\n</code></pre>\n<p>Rows with a string or number <code>id</code> property get stable table identity automatically. Use <code>getRowId</code> when identity lives somewhere else.</p>\n<p>The table above is visually headless: it inherits page colors and system defaults. For the polished stock look, <code>import &#39;ng-advanced-table/components/theme.css&#39;</code> once (see <code>/docs/theming</code>), or theme it yourself with <code>--nat-table-*</code> tokens.</p>\n<h2>Core-Only Scope</h2>\n<p>For a core-only table, provide <code>NatTableService</code> at the local wrapper that owns the table. Use <code>NatTableSurface</code> for the standard scoped controller and companion-control wiring.</p>\n<pre><code class="language-ts">import { Component } from &#39;@angular/core&#39;;\nimport { NatTable, NatTableService } from &#39;ng-advanced-table&#39;;\n\ninterface PositionRow {\n  id: string;\n  symbol: string;\n}\n\n@Component({\n  selector: &#39;app-core-only-table&#39;,\n  imports: [NatTable],\n  providers: [NatTableService],\n  template: ` &lt;nat-table [data]=&quot;rows&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Core-only positions&quot; /&gt; `\n})\nexport class CoreOnlyTable {\n  readonly rows: readonly PositionRow[] = [];\n  readonly columns = [\n    {\n      accessorKey: &#39;symbol&#39;,\n      header: &#39;Symbol&#39;,\n      meta: { label: &#39;Symbol&#39;, rowHeader: true }\n    }\n  ];\n}\n</code></pre>\n<h2>Add Companion Controls</h2>\n<p>Use <code>ng-advanced-table/components</code> when you want pagination controls, column visibility, horizontal scroll controls, toolbar behavior, header actions, selection checkbox columns, or export actions.</p>\n<pre><code class="language-ts">import { Component, signal } from &#39;@angular/core&#39;;\n\nimport { NatTable, type NatTableUserState } from &#39;ng-advanced-table&#39;;\nimport {\n  NatTableColumnVisibility,\n  NatTablePagination,\n  NatTableScrollControl,\n  NatTableSurface,\n  withNatTableHeaderActions\n} from &#39;ng-advanced-table/components&#39;;\n\ninterface PositionRow {\n  id: string;\n  symbol: string;\n  desk: string;\n  price: number;\n}\n\n@Component({\n  selector: &#39;app-positions-table&#39;,\n  imports: [NatTable, NatTableColumnVisibility, NatTablePagination, NatTableScrollControl, NatTableSurface],\n  template: `\n    &lt;nat-table-surface [initialState]=&quot;initialState&quot;&gt;\n      &lt;nat-table-pagination [pageSizeOptions]=&quot;[25, 50, 100]&quot; /&gt;\n\n      &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n\n      &lt;nat-table-scroll-control /&gt;\n      &lt;nat-table-column-visibility /&gt;\n    &lt;/nat-table-surface&gt;\n  `\n})\nexport class PositionsTable {\n  readonly rows = signal&lt;readonly PositionRow[]&gt;([]);\n  readonly initialState: Partial&lt;NatTableUserState&gt; = {\n    pagination: { pageIndex: 0, pageSize: 25 }\n  };\n\n  readonly columns = withNatTableHeaderActions([\n    {\n      accessorKey: &#39;symbol&#39;,\n      header: &#39;Symbol&#39;,\n      meta: { label: &#39;Symbol&#39;, rowHeader: true }\n    },\n    {\n      accessorKey: &#39;desk&#39;,\n      header: &#39;Desk&#39;,\n      meta: { label: &#39;Desk&#39; }\n    },\n    {\n      accessorKey: &#39;price&#39;,\n      header: &#39;Price&#39;,\n      meta: { label: &#39;Price&#39;, align: &#39;end&#39; },\n      cell: (context) =&gt; `$${context.getValue&lt;number&gt;().toFixed(2)}`\n    }\n  ]);\n}\n</code></pre>\n<p><code>NatTableSurface</code> provides the scoped table controller used by companion controls. Controls inside the same surface can read table state, patch state, bind <code>aria-controls</code>, and resolve locale labels without extra wiring.</p>\n<h2>Authoring Defaults</h2>\n<p>Start with these defaults unless the feature needs something else:</p>\n<table>\n<thead>\n<tr>\n<th>Need</th>\n<th>Default choice</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Table identity</td>\n<td><code>accessibleName</code> or visible <code>caption</code></td>\n</tr>\n<tr>\n<td>Row identity</td>\n<td>String or number <code>row.id</code>; <code>getRowId</code> for custom, composite, or nested identifiers</td>\n</tr>\n<tr>\n<td>Column labels</td>\n<td><code>meta.label</code> on every column</td>\n</tr>\n<tr>\n<td>Row header</td>\n<td><code>meta.rowHeader: true</code> on the primary identifying column</td>\n</tr>\n<tr>\n<td>Numeric columns</td>\n<td><code>meta.align: &#39;end&#39;</code></td>\n</tr>\n<tr>\n<td>Initial page size or sort</td>\n<td><code>[initialState]</code></td>\n</tr>\n<tr>\n<td>Persisting one state slice</td>\n<td><code>[state]</code> with only that slice plus the matching <code>*Change</code> output</td>\n</tr>\n<tr>\n<td>Search and domain filters</td>\n<td>Consumer-owned controls that patch table state</td>\n</tr>\n<tr>\n<td>Loading, empty, and error</td>\n<td><code>dataStatus</code> plus <code>natTableLoading</code>, <code>natTableEmpty</code>, or <code>natTableError</code> templates</td>\n</tr>\n</tbody></table>\n<h2>Next Steps</h2>\n<ul>\n<li>Use <code>/docs/columns</code> for column metadata, sizing, custom cell components, header actions, and row activation.</li>\n<li>Use <code>/docs/state</code> for controlled and uncontrolled state patterns.</li>\n<li>Use <code>/docs/data-lifecycle</code> for loading, empty, error, background refresh, and Manual Data Handling.</li>\n<li>Use <code>/docs/filtering-search</code> for search and filtering controls.</li>\n<li>Use <code>/docs/row-selection</code> for selection checkboxes and bulk state.</li>\n<li>Use <code>/docs/export</code> for CSV defaults and custom export handlers.</li>\n</ul>\n',
    headings: [
      {
        depth: 2,
        id: 'recommended-agent-setup'
      },
      {
        depth: 2,
        id: 'manual-setup'
      },
      {
        depth: 2,
        id: 'first-table'
      },
      {
        depth: 2,
        id: 'core-only-scope'
      },
      {
        depth: 2,
        id: 'add-companion-controls'
      },
      {
        depth: 2,
        id: 'authoring-defaults'
      },
      {
        depth: 2,
        id: 'next-steps'
      }
    ]
  },
  '/docs/render-metrics.md': {
    html: '<p>Render metrics are optional diagnostics from <code>ng-advanced-table/render-metrics</code>. They measure row render timing, add a synthetic metrics column, and expose a compact panel and filter. Enable them when you are tuning a heavy table or building an internal performance view.</p>\n<h2>Install</h2>\n<p>Install the table package and Angular companion peers:</p>\n<pre><code class="language-bash">pnpm add ng-advanced-table @angular/aria @angular/cdk\n# or: npm install ng-advanced-table @angular/aria @angular/cdk\n# or: yarn add ng-advanced-table @angular/aria @angular/cdk\n</code></pre>\n<p>Keep <code>@angular/core</code> and <code>@angular/common</code> in your Angular app dependencies.</p>\n<h2>Basic Wiring</h2>\n<p>Create one <code>NatTableRenderMetricsStore</code>, enable row render events on the table, and record each event.</p>\n<pre><code class="language-ts">import { Component, viewChild } from &#39;@angular/core&#39;;\n\nimport { NatTable } from &#39;ng-advanced-table&#39;;\nimport { NatTableSurface } from &#39;ng-advanced-table/components&#39;;\nimport { NatRenderMetricsPanel, NatTableRenderMetricsStore, type NatTableRenderMetricsEvent } from &#39;ng-advanced-table/render-metrics&#39;;\n\n@Component({\n  selector: &#39;app-positions-table&#39;,\n  imports: [NatTable, NatTableSurface, NatRenderMetricsPanel],\n  template: `\n    &lt;nat-table-surface&gt;\n      &lt;nat-render-metrics-panel [controller]=&quot;metricsTable()&quot; [store]=&quot;metricsStore&quot; /&gt;\n\n      &lt;nat-table\n        #metricsTable\n        [data]=&quot;rows()&quot;\n        [columns]=&quot;columns&quot;\n        [emitRowRenderEvents]=&quot;true&quot;\n        accessibleName=&quot;Position render metrics&quot;\n        (rowRendered)=&quot;onRowRendered($event)&quot; /&gt;\n    &lt;/nat-table-surface&gt;\n  `\n})\nexport class PositionsTable {\n  readonly metricsStore = new NatTableRenderMetricsStore();\n  readonly metricsTable = viewChild&lt;NatTable&lt;PositionRow&gt;&gt;(&#39;metricsTable&#39;);\n  readonly columns = [];\n\n  protected onRowRendered(event: NatTableRenderMetricsEvent): void {\n    this.metricsStore.record(event);\n  }\n}\n</code></pre>\n<p><code>emitRowRenderEvents</code> is off by default because it adds per-row render instrumentation. Keep it disabled in ordinary tables.</p>\n<h2>Metrics Column</h2>\n<p>Use <code>withRenderMetricsColumn(...)</code> to append a synthetic render-time column. Wrap with header actions after adding synthetic columns.</p>\n<pre><code class="language-ts">import { withNatTableHeaderActions } from &#39;ng-advanced-table/components&#39;;\nimport { NatTableRenderMetricsStore, withRenderMetricsColumn } from &#39;ng-advanced-table/render-metrics&#39;;\n\nreadonly metricsStore = new NatTableRenderMetricsStore();\n\nreadonly columns = withNatTableHeaderActions(\n  withRenderMetricsColumn(baseColumns, this.metricsStore, {\n    header: &#39;Render&#39;,\n    pendingLabel: &#39;Pending&#39;,\n    unitSuffix: &#39; ms&#39;,\n    size: 112,\n    minSize: 88,\n  }),\n);\n</code></pre>\n<p>The generated column:</p>\n<ul>\n<li>Uses id <code>__rowRenderMetric</code> unless <code>columnId</code> is provided.</li>\n<li>Aligns values to the end edge.</li>\n<li>Does not sort, hide, pin, or globally filter.</li>\n<li>Adds a column filter function driven by render tone.</li>\n<li>Shows a pending label until a row has been measured.</li>\n</ul>\n<h2>Panel And Filter</h2>\n<p>The panel summarizes the latest render cycle. The filter targets the synthetic metrics column.</p>\n<pre><code class="language-html">&lt;nat-table-surface&gt;\n  &lt;div class=&quot;render-metrics-controls&quot;&gt;\n    &lt;nat-render-metrics-filter [controller]=&quot;metricsTable()&quot; [store]=&quot;metricsStore&quot; /&gt;\n    &lt;nat-render-metrics-panel [controller]=&quot;metricsTable()&quot; [store]=&quot;metricsStore&quot; /&gt;\n  &lt;/div&gt;\n\n  &lt;nat-table\n    #metricsTable\n    [data]=&quot;rows()&quot;\n    [columns]=&quot;columns&quot;\n    [emitRowRenderEvents]=&quot;true&quot;\n    accessibleName=&quot;Position render metrics&quot;\n    (rowRendered)=&quot;metricsStore.record($event)&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p><code>NatRenderMetricsFilter</code> patches <code>columnFilters</code> for the metrics column and resets pagination to the first page. Pass the <code>NatTable</code> instance, or any object matching <code>NatTableRenderMetricsController</code>, through <code>[controller]</code>.</p>\n<p>Keep <code>NatRenderMetricsFilter</code> outside <code>&lt;nat-table-toolbar&gt;</code> because it renders its own internal chip buttons as a labeled button group. Use <code>&lt;nat-table-toolbar&gt;</code> for controls that can register each interactive element with <code>natToolbarItem</code> or <code>NatToolbarGroup</code>.</p>\n<h2>Reading Measurements</h2>\n<p>The store exposes read-only row-level and cycle-level metrics. Row history is bounded to the newest 1000 row ids by default so long-lived tables with row churn do not retain unbounded metrics.</p>\n<pre><code class="language-ts">readonly metricsStore = new NatTableRenderMetricsStore({\n  maxRetainedRowMetrics: 2000,\n});\n\nreadonly latestMeasurement = computed(() =&gt; this.metricsStore.measurement());\nreadonly retainedRowMetrics = computed(() =&gt; this.metricsStore.rowMetrics());\n\nprotected resetMetrics(): void {\n  this.metricsStore.reset();\n}\n\nprotected metricFor(rowId: string): RowRenderMetric | undefined {\n  return this.metricsStore.rowMetric(rowId);\n}\n</code></pre>\n<p>The latest measurement includes:</p>\n<table>\n<thead>\n<tr>\n<th>Field</th>\n<th>Meaning</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>durationMs</code></td>\n<td>Total visible render duration for the latest cycle</td>\n</tr>\n<tr>\n<td><code>averageRowDurationMs</code></td>\n<td>Mean row duration</td>\n</tr>\n<tr>\n<td><code>rowCount</code></td>\n<td>Number of visible rows sampled</td>\n</tr>\n<tr>\n<td><code>rowsPerSecond</code></td>\n<td>Approximate rendered rows per second</td>\n</tr>\n</tbody></table>\n<p>Row metrics include <code>durationMs</code>, <code>measuredAt</code>, and a derived tone: <code>fast</code> up to <code>12ms</code>, <code>watch</code> above <code>12ms</code>, or <code>slow</code> above <code>16.66ms</code>.</p>\n<p>Set <code>maxRetainedRowMetrics</code> to a higher finite value when a table needs a longer diagnostic window. Set it to <code>Infinity</code> only when the row-id space is known to be bounded.</p>\n<h2>Locale And Labels</h2>\n<p>Render-metrics copy can come from <code>ng-advanced-table/locale</code>.</p>\n<pre><code class="language-ts">import { provideNatTableRenderMetricsLocales } from &#39;ng-advanced-table/locale&#39;;\n\nproviders: [\n  provideNatTableRenderMetricsLocales({\n    en: {\n      renderMetrics: {\n        column: {\n          header: &#39;Render&#39;,\n          pendingLabel: &#39;Pending&#39;\n        },\n        panel: {\n          ariaLabel: &#39;Row render timing&#39;\n        },\n        filter: {\n          heading: &#39;Render speed&#39;,\n          groupAriaLabel: &#39;Filter rows by render speed&#39;\n        }\n      }\n    }\n  })\n];\n</code></pre>\n<p><code>withRenderMetricsColumn(...)</code> creates static column definitions. If the locale can change at runtime, rebuild the columns from a <code>computed(...)</code> value or pass the <code>locale</code> option when constructing them.</p>\n<pre><code class="language-ts">readonly columns = computed(() =&gt;\n  withRenderMetricsColumn(baseColumns(), this.metricsStore, {\n    locale: this.localeId(),\n  }),\n);\n</code></pre>\n<h2>Production Guidance</h2>\n<p>Use render metrics when they answer a specific performance question:</p>\n<ul>\n<li>Which rows are expensive to render?</li>\n<li>Did a custom cell renderer become slow?</li>\n<li>Did a live update pattern increase visible render time?</li>\n<li>Does a page-size change make row rendering unacceptable?</li>\n</ul>\n<p>Remove or gate metrics UI when it is no longer useful to the user. Keep <code>emitRowRenderEvents</code> off for normal production tables unless product requirements need the measurements.</p>\n',
    headings: [
      {
        depth: 2,
        id: 'install'
      },
      {
        depth: 2,
        id: 'basic-wiring'
      },
      {
        depth: 2,
        id: 'metrics-column'
      },
      {
        depth: 2,
        id: 'panel-and-filter'
      },
      {
        depth: 2,
        id: 'reading-measurements'
      },
      {
        depth: 2,
        id: 'locale-and-labels'
      },
      {
        depth: 2,
        id: 'production-guidance'
      }
    ]
  },
  '/docs/responsive-capabilities.md': {
    html: '<h2>The Computed-Columns Pattern</h2>\n<p>Responsive tables often need to disable specific capability UI on mobile while keeping it active on desktop: a header sort button and click-to-sort behavior might make room for an app-owned sort sheet, a resize handle might not fit a touch layout, and a pin menu might not be worth a tap target on small screens.</p>\n<p>The library stays viewport-agnostic. Feed any reactive condition — CDK <code>BreakpointObserver</code>, a <code>matchMedia</code> signal, a device service, a user preference — into the same column-composition APIs used everywhere else, and rebuild columns inside a <code>computed()</code> keyed on that signal.</p>\n<pre><code class="language-ts">readonly isMobile = toSignal(\n  inject(BreakpointObserver)\n    .observe(&#39;(max-width: 767px)&#39;)\n    .pipe(map((result) =&gt; result.matches)),\n  { initialValue: false }\n);\n\nreadonly columns = computed(() =&gt;\n  withNatTableHeaderActions(\n    baseColumns.map((column) =&gt; ({ ...column, enableResizing: !this.isMobile() })),\n    {\n      enableSortActions: !this.isMobile(),\n      enableColumnPinActions: !this.isMobile()\n    }\n  )\n);\n</code></pre>\n<p>When the breakpoint signal flips, the <code>computed</code> re-wraps the columns and the headers re-render. Applying <code>withNatTableHeaderActions(...)</code> repeatedly is safe: it unwraps previously wrapped headers before installing the next wrapper, so reactive column builders can compose it without nesting the generated controls.</p>\n<h2>Capability Opt-Out Table</h2>\n<table>\n<thead>\n<tr>\n<th>Capability</th>\n<th>Mobile opt-out</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Sort button and indicator</td>\n<td><code>enableSortActions: false</code> (helper option)</td>\n</tr>\n<tr>\n<td>Column resizing (handle, drag, keyboard)</td>\n<td><code>enableResizing: false</code> (columnDef)</td>\n</tr>\n<tr>\n<td>Pin menu</td>\n<td><code>enableColumnPinActions: false</code> (helper option)</td>\n</tr>\n<tr>\n<td>Reorder menu</td>\n<td><code>enableColumnReorderActions: false</code> (helper option)</td>\n</tr>\n<tr>\n<td>Programmatic sorting (<code>setSorting</code>, sort sheet)</td>\n<td>Never disabled</td>\n</tr>\n</tbody></table>\n<p><code>enableSortActions: false</code> removes the sort button and indicator from the header; there is no click or keyboard sort interaction because the control that would host it is gone. <code>aria-sort</code> on the header cell is unaffected — it announces the column&#39;s sort <em>state</em>, not its affordance, so screen readers still hear that a column is sorted when an app-owned control drove the sort. Per-column <code>meta.headerActions.enableSortActions</code> overrides the helper-level option for a single column, following the same resolution order as <code>enableColumnPinActions</code> and <code>enableColumnReorderActions</code>.</p>\n<p>TanStack sorting stays fully enabled behind <code>enableSortActions: false</code>: <code>table.setSorting(...)</code> and columnDef-level <code>enableSorting</code> behave exactly as before. This library never maps the mobile opt-out to TanStack&#39;s table-level <code>enableSorting</code> flag — see the Usage Boundary below for why.</p>\n<h2>Usage Boundary</h2>\n<p>Viewport detection belongs to the consuming application, not the table. Wire <code>BreakpointObserver</code>, a <code>matchMedia</code> signal, or a device/user-preference service into the computed-columns pattern above; the library adds no breakpoint config, media-query handling, or new dependency.</p>\n<p>Table-level <code>enableSorting</code> is intentionally never used to implement this opt-out. A runtime check against the installed <code>@tanstack/table-core</code> shows that with <code>enableSorting: false</code>, <code>table.setSorting(...)</code> writes the sorting state but <code>getSortedRowModel</code> silently drops it — rows never sort, because the row model filters sorting state by <code>getCanSort()</code>, which returns <code>false</code> when the table-level flag is off. Mapping the mobile opt-out to that flag would silently break any app-owned sort sheet that sorts programmatically. <code>enableSortActions</code> only ever touches the header action UI.</p>\n',
    headings: [
      {
        depth: 2,
        id: 'the-computed-columns-pattern'
      },
      {
        depth: 2,
        id: 'capability-opt-out-table'
      },
      {
        depth: 2,
        id: 'usage-boundary'
      }
    ]
  },
  '/docs/row-selection.md': {
    html: '<h2>When To Use Selection</h2>\n<p>Use row selection when users need to compare, batch, or act on rows. Do not use selection for ordinary row activation; row activation and selection can coexist but they represent different intents.</p>\n<h2>Selection State</h2>\n<p>Selection is stored by stable row id in the <code>rowSelection</code> state slice. Rows with a string or number <code>id</code> property use that value automatically. Provide <code>getRowId</code> when identity is composite or lives elsewhere.</p>\n<pre><code class="language-ts">readonly tableState = signal&lt;Partial&lt;NatTableUserState&gt;&gt;({\n  rowSelection: {\n    &#39;position-1&#39;: true,\n  },\n});\n</code></pre>\n<pre><code class="language-html">&lt;nat-table-surface [(state)]=&quot;tableState&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; [enableRowSelection]=&quot;true&quot; accessibleName=&quot;Selectable positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p>Keep <code>rowSelection</code> in app state when bulk actions, URL persistence, or saved views need to survive table rerenders. Clear stale selected ids when the backing data set changes and selected rows no longer exist.</p>\n<h2>Selection Column</h2>\n<p>Use <code>withNatTableSelectionColumn(...)</code> for the standard leading checkbox column. Pair the helper with <code>[enableRowSelection]=&quot;true&quot;</code> on <code>&lt;nat-table&gt;</code>.</p>\n<pre><code class="language-ts">import { withNatTableSelectionColumn } from &#39;ng-advanced-table/components&#39;;\n\nreadonly columns = withNatTableSelectionColumn(baseColumns, {\n  label: &#39;Select rows&#39;,\n  selectAllAriaLabel: &#39;Select all positions&#39;,\n  selectRowAriaLabel: (row) =&gt; `Select ${row.original.symbol}`,\n});\n</code></pre>\n<p>The helper prepends a non-sortable, non-hideable, non-resizable column. It defaults to a 48px width, can be pinned, and uses generated locale labels unless you pass explicit overrides. Pin it with the normal <code>columnPinning</code> state when selection should stay visible during horizontal scrolling.</p>\n<h2>Single And Multiple Selection</h2>\n<p>Multiple selection is the default. Use <code>selectionMode=&quot;single&quot;</code> only when the workflow allows one selected row at a time. Clear or normalize app-owned selection when switching modes.</p>\n<pre><code class="language-html">&lt;nat-table\n  [data]=&quot;rows()&quot;\n  [columns]=&quot;columns&quot;\n  [enableRowSelection]=&quot;true&quot;\n  selectionMode=&quot;single&quot;\n  accessibleName=&quot;Selectable positions&quot; /&gt;\n</code></pre>\n<p>In single-selection mode, the generated selection column renders the header as a plain column label instead of a select-all checkbox. Select-all is only meaningful for multiple selection.</p>\n<h2>Direct Selection Checkboxes</h2>\n<p>Most tables should use <code>withNatTableSelectionColumn(...)</code>. Use <code>NatTableSelectionCheckbox</code> directly only when you are building a custom selection column or need to place the checkbox in a custom header or cell renderer.</p>\n<pre><code class="language-ts">import { flexRenderComponent } from &#39;ng-advanced-table&#39;;\nimport { NatTableSelectionCheckbox } from &#39;ng-advanced-table/components&#39;;\n\nreadonly columns = [\n  {\n    id: &#39;select&#39;,\n    header: (context) =&gt;\n      flexRenderComponent(NatTableSelectionCheckbox, {\n        inputs: {\n          mode: &#39;all&#39;,\n          table: context.table,\n          label: &#39;Select rows&#39;,\n        },\n      }),\n    cell: (context) =&gt;\n      flexRenderComponent(NatTableSelectionCheckbox, {\n        inputs: {\n          mode: &#39;row&#39;,\n          table: context.table,\n          row: context.row,\n          ariaLabel: `Select ${context.row.original.symbol}`,\n        },\n      }),\n  },\n  ...baseColumns,\n];\n</code></pre>\n<p>The checkbox component expects the table instance in both modes and the current <code>row</code> in row mode. Prefer locale providers for shared generated labels; pass <code>ariaLabel</code> only for table-specific copy.</p>\n<h2>Bulk Actions</h2>\n<p>Bulk controls belong to the consuming application. Place them in a toolbar when they are table commands, but keep domain side effects such as deleting rows, opening dialogs, or calling APIs outside the table.</p>\n<h2>Accessibility Notes</h2>\n<p>Rows expose <code>aria-selected</code> while selection is enabled. Multiple selection sets <code>aria-multiselectable=&quot;true&quot;</code> on the grid, and selection changes are announced through the table live region.</p>\n',
    headings: [
      {
        depth: 2,
        id: 'when-to-use-selection'
      },
      {
        depth: 2,
        id: 'selection-state'
      },
      {
        depth: 2,
        id: 'selection-column'
      },
      {
        depth: 2,
        id: 'single-and-multiple-selection'
      },
      {
        depth: 2,
        id: 'direct-selection-checkboxes'
      },
      {
        depth: 2,
        id: 'bulk-actions'
      },
      {
        depth: 2,
        id: 'accessibility-notes'
      }
    ]
  },
  '/docs/sorting.md': {
    html: '<h2>When To Use Sorting</h2>\n<p>Use sorting when the table can order the rows it currently receives, or when your app wants to expose sort state to Manual Data Handling. Keep domain ranking rules in the consuming app when sorting depends on permissions, remote scoring, or data that is not present in the current row set.</p>\n<h2>Controlled Sorting</h2>\n<p>Sorting lives in the <code>sorting</code> state slice. Let the table manage it for simple client-side tables, or own the slice when you need URL persistence, custom buttons, analytics, or a manual row pipeline.</p>\n<pre><code class="language-ts">readonly tableState = signal&lt;Partial&lt;NatTableUserState&gt;&gt;({\n  sorting: [{ id: &#39;name&#39;, desc: false }]\n});\n</code></pre>\n<p>Header sort controls are added by wrapping columns with <code>withNatTableHeaderActions(...)</code>. Programmatic controls should update the same state slice instead of keeping a second sort model.</p>\n<h2>Multi-Column Sorting</h2>\n<p>Enable multi-sort on the surface when users need priority order across multiple columns. The first sorting entry has the highest priority. Keep the priority visible when the workflow depends on it.</p>\n<pre><code class="language-html">&lt;nat-table-surface [enableMultiSort]=&quot;true&quot; [(state)]=&quot;tableState&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<pre><code class="language-ts">readonly tableState = signal&lt;Partial&lt;NatTableUserState&gt;&gt;({\n  sorting: [\n    { id: &#39;sector&#39;, desc: false },\n    { id: &#39;marketValue&#39;, desc: true },\n  ],\n});\n</code></pre>\n<p>Use the <code>sorting</code> array as the source of truth for priority. Header sort buttons add another sorted column when the user holds <code>Shift</code> while clicking, or <code>Shift</code> while pressing <code>Enter</code> on a focused sort button. Programmatic presets should write the same ordered array instead of keeping a separate priority model.</p>\n<p>When multi-sort changes the meaning of the result set, show the priority near the table or in the sorted headers. The bundled header actions render a priority badge for sorted columns when more than one column is active.</p>\n<h2>Custom Sort Indicators</h2>\n<p>Use <code>withNatTableHeaderActions(...)</code> when you want the bundled sort behavior, labels, and menu actions, but need indicator content that matches your product or design system.</p>\n<p>Keep the table header structure intact. The composable pattern is to leave each column&#39;s label or component in <code>column.header</code>, wrap the final columns with <code>withNatTableHeaderActions(...)</code>, and pass replacement icon or badge content through <code>sortIndicator</code>. Do not add another header row, style a row to look like a header, or wire a separate sort button just to change the icon.</p>\n<pre><code class="language-ts">import { Component, input } from &#39;@angular/core&#39;;\nimport { flexRenderComponent, type NatTableSortIndicatorContext } from &#39;ng-advanced-table&#39;;\nimport { withNatTableHeaderActions } from &#39;ng-advanced-table/components&#39;;\n\n@Component({\n  selector: &#39;app-sort-indicator&#39;,\n  template: `\n    &lt;span aria-hidden=&quot;true&quot; [attr.data-sort-state]=&quot;context().sortState || &#39;none&#39;&quot; class=&quot;sort-indicator&quot;&gt;\n      {{ context().sortState === &#39;asc&#39; ? &#39;Asc&#39; : context().sortState === &#39;desc&#39; ? &#39;Desc&#39; : &#39;Sort&#39; }}\n    &lt;/span&gt;\n  `,\n})\nexport class SortIndicator {\n  readonly context = input.required&lt;NatTableSortIndicatorContext&gt;();\n}\n\nreadonly columns = withNatTableHeaderActions(baseColumns, {\n  sortIndicator: (context) =&gt;\n    flexRenderComponent(SortIndicator, {\n      inputs: { context },\n    }),\n});\n</code></pre>\n<p>The indicator receives the current <code>sortState</code>, resolved column <code>label</code>, <code>ariaSort</code> token, and column object. Keep the indicator visual only; the generated sort button still owns the click handler, keyboard behavior, accessible name, multi-sort state, and <code>aria-sort</code>.</p>\n<p>Override individual columns through <code>column.meta.headerActions</code> when one column needs a different indicator or should opt out.</p>\n<pre><code class="language-ts">{\n  accessorKey: &#39;marketValue&#39;,\n  header: &#39;Market value&#39;,\n  meta: {\n    label: &#39;Market value&#39;,\n    align: &#39;end&#39;,\n    headerActions: {\n      sortIndicator: ({ sortState }) =&gt; (sortState === false ? &#39;No sort&#39; : sortState.toUpperCase()),\n    },\n  },\n}\n</code></pre>\n<p>Set <code>meta.headerActions</code> to <code>false</code> for action columns or utility columns that should keep their original header instead of receiving sort, pin, or move controls.</p>\n<h2>Pinned Columns Variant</h2>\n<p>Sorting and pinning can coexist. Pinning affects where columns render; sorting still uses column ids and row data. Keep pinned columns stable when they carry row identity or row actions.</p>\n<h2>Hiding Sort UI Does Not Disable Sorting</h2>\n<p><code>withNatTableHeaderActions(columns, { enableSortActions: false })</code> removes the sort button and indicator from the header, but sorting itself keeps working: <code>table.setSorting(...)</code> and columnDef-level <code>enableSorting</code> are unaffected, and <code>aria-sort</code> still reflects the applied sort. This is the mechanism behind hiding capability UI on mobile while an app-owned control (a sort sheet, for example) drives sorting programmatically. See the Responsive Capabilities topic for the full pattern.</p>\n',
    headings: [
      {
        depth: 2,
        id: 'when-to-use-sorting'
      },
      {
        depth: 2,
        id: 'controlled-sorting'
      },
      {
        depth: 2,
        id: 'multi-column-sorting'
      },
      {
        depth: 2,
        id: 'custom-sort-indicators'
      },
      {
        depth: 2,
        id: 'pinned-columns-variant'
      },
      {
        depth: 2,
        id: 'hiding-sort-ui-does-not-disable-sorting'
      }
    ]
  },
  '/docs/state.md': {
    html: '<p><code>NatTable</code> can own all table state, or your app can control only the slices it cares about. The most important rule is simple: a state slice is controlled only when its property is present in <code>[state]</code>. Omitted slices stay internal.</p>\n<h2>State Slices</h2>\n<p><code>NatTableUserState</code> contains the serializable view state exposed by the table and companion surface.</p>\n<table>\n<thead>\n<tr>\n<th>Slice</th>\n<th>Use it for</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>sorting</code></td>\n<td>Active sort entries</td>\n</tr>\n<tr>\n<td><code>globalFilter</code></td>\n<td>Current global search query</td>\n</tr>\n<tr>\n<td><code>columnFilters</code></td>\n<td>Column-specific filters keyed by column id</td>\n</tr>\n<tr>\n<td><code>pagination</code></td>\n<td><code>pageIndex</code> and <code>pageSize</code></td>\n</tr>\n<tr>\n<td><code>columnVisibility</code></td>\n<td>Hideable column visibility map</td>\n</tr>\n<tr>\n<td><code>columnOrder</code></td>\n<td>Leaf-column order</td>\n</tr>\n<tr>\n<td><code>columnPinning</code></td>\n<td>Left and right pinned column ids</td>\n</tr>\n<tr>\n<td><code>columnSizing</code></td>\n<td>Resized column widths in pixels</td>\n</tr>\n<tr>\n<td><code>rowSelection</code></td>\n<td>Selected row ids keyed by <code>getRowId</code>, <code>row.id</code>, or the namespaced positional fallback</td>\n</tr>\n</tbody></table>\n<p>The <code>pagination</code> slice always exists in emitted state. Pagination affects rendered rows only when pagination is enabled by a pagination companion control or a table setup that registers pagination.</p>\n<h2>Start Uncontrolled</h2>\n<p>Most tables should start uncontrolled. Pass <code>[initialState]</code> for defaults and let the table manage updates internally.</p>\n<pre><code class="language-ts">readonly initialState: Partial&lt;NatTableUserState&gt; = {\n  pagination: { pageIndex: 0, pageSize: 25 },\n  sorting: [{ id: &#39;symbol&#39;, desc: false }],\n  columnPinning: { left: [&#39;symbol&#39;], right: [] },\n};\n</code></pre>\n<pre><code class="language-html">&lt;nat-table-surface [initialState]=&quot;initialState&quot;&gt;\n  &lt;nat-table-pagination [pageSizeOptions]=&quot;[25, 50, 100]&quot; /&gt;\n\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p><code>initialState</code> is read as a seed. It is not a live binding for subsequent resets. If a slice also appears in <code>[state]</code>, the controlled value wins.</p>\n<h2>Own One Slice</h2>\n<p>When your application needs to persist, synchronize, or inspect one state slice, control only that slice and listen to the matching granular output.</p>\n<pre><code class="language-ts">import { Component, computed, signal } from &#39;@angular/core&#39;;\nimport type { NatTableUserState, PaginationState, SortingState, VisibilityState } from &#39;ng-advanced-table&#39;;\n\nexport class PositionsTable {\n  readonly sorting = signal&lt;SortingState&gt;([]);\n\n  readonly tableState = computed&lt;Partial&lt;NatTableUserState&gt;&gt;(() =&gt; ({\n    sorting: this.sorting()\n  }));\n\n  protected onSortingChange(sorting: SortingState): void {\n    this.sorting.set(sorting);\n  }\n}\n</code></pre>\n<pre><code class="language-html">&lt;nat-table-surface [state]=&quot;tableState()&quot; (sortingChange)=&quot;onSortingChange($event)&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p>This keeps sorting controlled and leaves filters, pagination, pinning, sizing, order, visibility, and selection internal.</p>\n<h2>Own Multiple Slices</h2>\n<p>Use one signal when several slices belong to the same workflow, such as URL persistence or a saved table view.</p>\n<pre><code class="language-ts">readonly viewState = signal&lt;Partial&lt;NatTableUserState&gt;&gt;({\n  sorting: [{ id: &#39;symbol&#39;, desc: false }],\n  columnVisibility: { desk: true, exchange: false },\n  pagination: { pageIndex: 0, pageSize: 25 },\n});\n\nprotected onPaginationChange(pagination: PaginationState): void {\n  this.viewState.update((state) =&gt; ({ ...state, pagination }));\n}\n\nprotected onColumnVisibilityChange(columnVisibility: VisibilityState): void {\n  this.viewState.update((state) =&gt; ({ ...state, columnVisibility }));\n}\n</code></pre>\n<pre><code class="language-html">&lt;nat-table-surface\n  [state]=&quot;viewState()&quot;\n  (paginationChange)=&quot;onPaginationChange($event)&quot;\n  (columnVisibilityChange)=&quot;onColumnVisibilityChange($event)&quot;&gt;\n  &lt;nat-table-pagination [pageSizeOptions]=&quot;[25, 50, 100]&quot; /&gt;\n  &lt;nat-table-column-visibility /&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p>Prefer granular outputs when the app controls known slices. Use <code>(stateChange)</code> when you intentionally need a broad table state snapshot.</p>\n<h2>Full State Snapshots</h2>\n<p><code>(stateChange)</code> is typed as <code>Partial&lt;NatTableUserState&gt;</code> because it pairs with the partial <code>[state]</code> input. The emitted value is useful for telemetry, local storage, or &quot;save current view&quot; commands.</p>\n<pre><code class="language-ts">readonly latestState = signal&lt;Partial&lt;NatTableUserState&gt; | null&gt;(null);\n\nprotected rememberState(state: Partial&lt;NatTableUserState&gt;): void {\n  this.latestState.set(state);\n}\n</code></pre>\n<pre><code class="language-html">&lt;nat-table-surface (stateChange)=&quot;rememberState($event)&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p>Be careful with two-way full-state binding:</p>\n<pre><code class="language-html">&lt;nat-table-surface [(state)]=&quot;tableState&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Open positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<p>After the first emitted state is assigned back into <code>[state]</code>, every slice is present and therefore every slice becomes controlled. Use this only when that is what you want.</p>\n<h2>URL Persistence</h2>\n<p>For URL-backed views, store only the slices that should survive reloads. Keep volatile slices such as <code>columnSizing</code> or <code>rowSelection</code> out unless the product specifically needs them.</p>\n<pre><code class="language-ts">readonly stateFromUrl = signal&lt;Partial&lt;NatTableUserState&gt;&gt;({\n  sorting: [{ id: &#39;price&#39;, desc: true }],\n  pagination: { pageIndex: 0, pageSize: 50 },\n});\n\nprotected onSortingChange(sorting: SortingState): void {\n  this.stateFromUrl.update((state) =&gt; ({ ...state, sorting, pagination: firstPage(state) }));\n  this.writeUrl();\n}\n\nprotected onPaginationChange(pagination: PaginationState): void {\n  this.stateFromUrl.update((state) =&gt; ({ ...state, pagination }));\n  this.writeUrl();\n}\n\nprivate writeUrl(): void {\n  const state = this.stateFromUrl();\n  // Serialize only the slices that are part of the route contract.\n}\n\nfunction firstPage(state: Partial&lt;NatTableUserState&gt;): PaginationState {\n  const pagination = state.pagination ?? { pageIndex: 0, pageSize: 25 };\n\n  return { ...pagination, pageIndex: 0 };\n}\n</code></pre>\n<p>Global filter and column-filter updates reset <code>pagination.pageIndex</code> to <code>0</code> inside the table. If your app owns filter slices and pagination together, mirror that behavior when updating your signal.</p>\n<h2>Manual Data Handling</h2>\n<p>Use manual mode when your app owns sorting, filtering, or pagination outside the table. The table still emits state changes; your container prepares rows and passes back the current page or current row set.</p>\n<pre><code class="language-html">&lt;nat-table-surface\n  [mode]=&quot;{ pagination: &#39;manual&#39;, sorting: &#39;manual&#39;, filtering: &#39;manual&#39; }&quot;\n  [manualPageCount]=&quot;pageCount()&quot;\n  [state]=&quot;tableState()&quot;\n  (stateChange)=&quot;loadPage($event)&quot;&gt;\n  &lt;nat-table-pagination [pageSizeOptions]=&quot;[25, 50, 100]&quot; /&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; [dataStatus]=&quot;status()&quot; [error]=&quot;error()&quot; accessibleName=&quot;Manual positions&quot; /&gt;\n&lt;/nat-table-surface&gt;\n</code></pre>\n<pre><code class="language-ts">protected loadPage(state: Partial&lt;NatTableUserState&gt;): void {\n  this.tableState.set(state);\n  this.status.set(NAT_TABLE_DATA_STATUS.loading);\n  const pagination = state.pagination ?? { pageIndex: 0, pageSize: 25 };\n\n  this.positionsApi\n    .list({\n      pageIndex: pagination.pageIndex,\n      pageSize: pagination.pageSize,\n      sorting: state.sorting ?? [],\n      globalFilter: state.globalFilter ?? &#39;&#39;,\n      columnFilters: state.columnFilters ?? [],\n    })\n    .subscribe({\n      next: (result) =&gt; {\n        this.rows.set(result.rows);\n        this.pageCount.set(result.pageCount);\n        this.status.set(NAT_TABLE_DATA_STATUS.success);\n      },\n      error: (error: unknown) =&gt; {\n        this.error.set(error);\n        this.status.set(NAT_TABLE_DATA_STATUS.error);\n      },\n    });\n}\n</code></pre>\n<p>You can also mix modes. For example, <code>{ pagination: &#39;manual&#39;, sorting: &#39;auto&#39; }</code> prepares pages externally while sorting the rows currently held by the client.</p>\n<h2>Stable Row IDs</h2>\n<p>Rows with a string or number <code>id</code> property get stable ids automatically.</p>\n<pre><code class="language-ts">interface PositionRow {\n  id: string;\n  symbol: string;\n}\n</code></pre>\n<p>Provide <code>getRowId</code> when the stable id lives under another property, is composite, or needs parent-aware nested row keys.</p>\n<pre><code class="language-ts">interface PositionRow {\n  accountId: string;\n  positionId: string;\n  symbol: string;\n}\n\nreadonly getRowId = (row: PositionRow) =&gt; `${row.accountId}:${row.positionId}`;\n</code></pre>\n<p>Without stable IDs, the table falls back to namespaced row positions. Selection, row activation, and per-row instrumentation can then follow indexes instead of the underlying data after sorting, filtering, paging, or refreshes.</p>\n',
    headings: [
      {
        depth: 2,
        id: 'state-slices'
      },
      {
        depth: 2,
        id: 'start-uncontrolled'
      },
      {
        depth: 2,
        id: 'own-one-slice'
      },
      {
        depth: 2,
        id: 'own-multiple-slices'
      },
      {
        depth: 2,
        id: 'full-state-snapshots'
      },
      {
        depth: 2,
        id: 'url-persistence'
      },
      {
        depth: 2,
        id: 'manual-data-handling'
      },
      {
        depth: 2,
        id: 'stable-row-ids'
      }
    ]
  },
  '/docs/theming.md': {
    html: '<p>The table theme contract is CSS custom properties. Put tokens on an ancestor of the table and let inheritance flow through the core table, companion controls, and optional render-metrics UI.</p>\n<h2>Recommended Shape</h2>\n<p>Use <code>NatTableSurface</code> when you want the shared controller scope and companion-control wiring. The core table is visually headless by default; import the opt-in stock theme (see Opt-in Stock Theme) or scope inherited <code>--nat-table-*</code> custom properties on a wrapper around the table, controls, and optional render-metrics UI.</p>\n<pre><code class="language-html">&lt;section class=&quot;orders-table-theme&quot;&gt;\n  &lt;nat-table-surface&gt;\n    &lt;nat-table-pagination [pageSizeOptions]=&quot;[25, 50, 100]&quot; /&gt;\n\n    &lt;nat-table #table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Orders&quot; /&gt;\n\n    &lt;nat-table-scroll-control /&gt;\n    &lt;nat-table-column-visibility /&gt;\n    &lt;nat-render-metrics-panel [controller]=&quot;table&quot; [store]=&quot;renderMetricsStore&quot; /&gt;\n  &lt;/nat-table-surface&gt;\n&lt;/section&gt;\n</code></pre>\n<p>Start with the semantic table palette. The table, bundled controls, and render-metrics components inherit these values.</p>\n<pre><code class="language-css">.orders-table-theme {\n  --nat-table-color-text: #111827;\n  --nat-table-color-text-muted: #4b5563;\n  --nat-table-color-accent: #2563eb;\n  --nat-table-color-success: #166534;\n  --nat-table-color-warning: #a16207;\n  --nat-table-color-danger: #b91c1c;\n  --nat-table-color-surface: #ffffff;\n  --nat-table-color-surface-elevated: #ffffff;\n  --nat-table-color-surface-sticky: #f9fafb;\n\n  color-scheme: light;\n}\n\n[data-theme=&#39;dark&#39;] .orders-table-theme {\n  --nat-table-color-text: #f9fafb;\n  --nat-table-color-text-muted: #cbd5e1;\n  --nat-table-color-accent: #60a5fa;\n  --nat-table-color-success: #86efac;\n  --nat-table-color-warning: #fde68a;\n  --nat-table-color-danger: #fca5a5;\n  --nat-table-color-surface: rgb(15 23 42 / 92%);\n  --nat-table-color-surface-elevated: #111827;\n  --nat-table-color-surface-sticky: #1f2937;\n\n  color-scheme: dark;\n}\n</code></pre>\n<p>Use direct <code>--nat-table-*</code> overrides when the table needs a table-specific decision that should not affect the rest of the app.</p>\n<pre><code class="language-css">.orders-table-theme {\n  --nat-table-header-background: #eef2ff;\n  --nat-table-row-background-hover: #f8fafc;\n  --nat-table-focus-ring-color: #1d4ed8;\n  --nat-table-radius-chip: 0.75rem;\n}\n</code></pre>\n<h2>Theme Scope</h2>\n<p>Choose the narrowest scope that matches the product decision.</p>\n<pre><code class="language-css">.risk-table {\n  --nat-table-color-accent: #7c3aed;\n  --nat-table-header-background: #f5f3ff;\n  --nat-table-pinned-divider-color: #c4b5fd;\n}\n</code></pre>\n<p>For app-wide themes, put table tokens on the app root or theme attribute.</p>\n<pre><code class="language-css">:root {\n  --nat-table-color-text: #111827;\n  --nat-table-color-text-muted: #6b7280;\n  --nat-table-color-accent: #2563eb;\n  --nat-table-color-surface: #ffffff;\n  --nat-table-color-surface-elevated: #ffffff;\n  --nat-table-color-surface-sticky: #f9fafb;\n  --nat-table-color-success: #15803d;\n  --nat-table-color-warning: #a16207;\n  --nat-table-color-danger: #b91c1c;\n\n  color-scheme: light;\n}\n\n[data-theme=&#39;dark&#39;] {\n  --nat-table-color-text: #f9fafb;\n  --nat-table-color-text-muted: #9ca3af;\n  --nat-table-color-accent: #60a5fa;\n  --nat-table-color-surface: #111827;\n  --nat-table-color-surface-elevated: #1f2937;\n  --nat-table-color-surface-sticky: #374151;\n  --nat-table-color-success: #86efac;\n  --nat-table-color-warning: #fde68a;\n  --nat-table-color-danger: #fca5a5;\n\n  color-scheme: dark;\n}\n</code></pre>\n<p>If controls and the table live in different surfaces, put product tokens on their shared ancestor so both surfaces derive the same theme.</p>\n<p>The live example below scopes <code>--nat-table-*</code> tokens on <code>NatTableSurface</code>, with a matching <code>[data-theme=&#39;dark&#39;]</code> override. It intentionally avoids private component classes and <code>::ng-deep</code>, so the same CSS can live in a consumer app stylesheet.</p>\n<h2>Core Table Tokens</h2>\n<p>These are the most common stable <code>ng-advanced-table</code> tokens to override directly.</p>\n<table>\n<thead>\n<tr>\n<th>Token</th>\n<th>Purpose</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>--nat-table-color-text</code></td>\n<td>Base table text color</td>\n</tr>\n<tr>\n<td><code>--nat-table-color-border</code></td>\n<td>Shared surface and control border</td>\n</tr>\n<tr>\n<td><code>--nat-table-color-divider</code></td>\n<td>Shared row and cell divider</td>\n</tr>\n<tr>\n<td><code>--nat-table-font-family</code></td>\n<td>Table font family</td>\n</tr>\n<tr>\n<td><code>--nat-table-region-background</code></td>\n<td>Scrollable table region background</td>\n</tr>\n<tr>\n<td><code>--nat-table-region-border-color</code></td>\n<td>Scrollable table region border</td>\n</tr>\n<tr>\n<td><code>--nat-table-region-border-width</code></td>\n<td>Scrollable table region border width</td>\n</tr>\n<tr>\n<td><code>--nat-table-radius-region</code></td>\n<td>Scrollable table region radius</td>\n</tr>\n<tr>\n<td><code>--nat-table-header-background</code></td>\n<td>Header background</td>\n</tr>\n<tr>\n<td><code>--nat-table-header-color</code></td>\n<td>Header text color</td>\n</tr>\n<tr>\n<td><code>--nat-table-font-weight-header</code></td>\n<td>Header text font weight</td>\n</tr>\n<tr>\n<td><code>--nat-table-header-border-color</code></td>\n<td>Header divider</td>\n</tr>\n<tr>\n<td><code>--nat-table-header-border-width</code></td>\n<td>Header divider width</td>\n</tr>\n<tr>\n<td><code>--nat-table-row-background</code></td>\n<td>Default body row background</td>\n</tr>\n<tr>\n<td><code>--nat-table-row-background-hover</code></td>\n<td>Hovered row background</td>\n</tr>\n<tr>\n<td><code>--nat-table-row-background-focus</code></td>\n<td>Focus-within row background</td>\n</tr>\n<tr>\n<td><code>--nat-table-pinned-background</code></td>\n<td>Pinned cell background</td>\n</tr>\n<tr>\n<td><code>--nat-table-pinned-header-background</code></td>\n<td>Pinned header cell background (falls back to <code>--nat-table-pinned-background</code>)</td>\n</tr>\n<tr>\n<td><code>--nat-table-pinned-divider-color</code></td>\n<td>Pinned edge divider</td>\n</tr>\n<tr>\n<td><code>--nat-table-cell-border-color</code></td>\n<td>Body cell divider</td>\n</tr>\n<tr>\n<td><code>--nat-table-cell-border-width</code></td>\n<td>Body cell divider width</td>\n</tr>\n<tr>\n<td><code>--nat-table-cell-color-positive</code></td>\n<td>Positive semantic cell tone</td>\n</tr>\n<tr>\n<td><code>--nat-table-cell-color-negative</code></td>\n<td>Negative semantic cell tone</td>\n</tr>\n<tr>\n<td><code>--nat-table-cell-color-warning</code></td>\n<td>Warning semantic cell tone</td>\n</tr>\n<tr>\n<td><code>--nat-table-cell-color-neutral</code></td>\n<td>Neutral semantic cell tone</td>\n</tr>\n<tr>\n<td><code>--nat-table-empty-state-color</code></td>\n<td>Empty, loading, and error row text</td>\n</tr>\n<tr>\n<td><code>--nat-table-focus-ring-color</code></td>\n<td>Keyboard focus indicator</td>\n</tr>\n<tr>\n<td><code>--nat-table-focus-ring-width</code></td>\n<td>Keyboard focus indicator width</td>\n</tr>\n<tr>\n<td><code>--nat-table-space-cell-y</code></td>\n<td>Cell block padding</td>\n</tr>\n<tr>\n<td><code>--nat-table-space-cell-x</code></td>\n<td>Cell inline padding (shared base)</td>\n</tr>\n<tr>\n<td><code>--nat-table-space-header-cell-x</code></td>\n<td>Header cell inline padding (falls back to <code>--nat-table-space-cell-x</code>)</td>\n</tr>\n<tr>\n<td><code>--nat-table-space-data-cell-x</code></td>\n<td>Data cell inline padding (falls back to <code>--nat-table-space-cell-x</code>)</td>\n</tr>\n</tbody></table>\n<p>Set the border-width tokens to <code>0</code> when a design needs to remove the outer table boundary or internal dividers.</p>\n<h2>UI Control Tokens</h2>\n<p><code>ng-advanced-table/components</code> controls consume the same theme. Common groups include:</p>\n<table>\n<thead>\n<tr>\n<th>Group</th>\n<th>Common tokens</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Surface</td>\n<td><code>--nat-table-card-background</code>, <code>--nat-table-card-border-color</code>, <code>--nat-table-card-shadow</code>, <code>--nat-table-radius-card</code>, <code>--nat-table-space-card</code></td>\n</tr>\n<tr>\n<td>Controls</td>\n<td><code>--nat-table-color-text-muted</code>, <code>--nat-table-font-size-label</code>, <code>--nat-table-space-control-block-gap</code></td>\n</tr>\n<tr>\n<td>Chips</td>\n<td><code>--nat-table-chip-background</code>, <code>--nat-table-chip-background-active</code>, <code>--nat-table-chip-border-color</code>, <code>--nat-table-radius-chip</code></td>\n</tr>\n<tr>\n<td>Pager</td>\n<td><code>--nat-table-pager-background</code>, <code>--nat-table-pager-border-color</code>, <code>--nat-table-pager-color</code>, <code>--nat-table-pager-min-height</code></td>\n</tr>\n<tr>\n<td>Header actions</td>\n<td><code>--nat-table-sort-icon-color-active</code>, <code>--nat-table-sort-icon-color-idle</code>, <code>--nat-table-pin-color-pinned</code></td>\n</tr>\n<tr>\n<td>Motion</td>\n<td><code>--nat-table-transition-fast</code>, <code>--nat-table-transition-medium</code>, <code>--nat-table-disabled-opacity</code></td>\n</tr>\n<tr>\n<td>Stacking</td>\n<td><code>--nat-table-z-index-sticky-header</code>, <code>--nat-table-z-index-pinned-cell</code>, <code>--nat-table-z-index-drag-preview</code></td>\n</tr>\n</tbody></table>\n<p>Reach for these direct tokens when a table control needs a local exception.</p>\n<p>Render-metrics widgets intentionally do not expose their own component-specific tokens. They inherit the shared semantic palette (<code>--nat-table-color-*</code>) so they remain readable in light and dark themes, while their compact KPI/chip styling stays internal.</p>\n<h2>Opt-in Stock Theme</h2>\n<p><code>ng-advanced-table</code> core is headless by default: with no theme applied, the table and companion controls render with conservative system-color fallbacks and inherit the page&#39;s colors. Neither the core table nor <code>NatTableSurface</code> ships the stock look as a built-in default.</p>\n<p>To get the polished stock look, import the opt-in stylesheet once, for example in your app&#39;s global styles or <code>main.ts</code>:</p>\n<pre><code class="language-ts">import &#39;ng-advanced-table/components/theme.css&#39;;\n</code></pre>\n<p>The stock theme is light/dark responsive: its palette uses <code>light-dark()</code> and the theme sets <code>color-scheme: light dark</code> on <code>:root</code>, so it follows the operating-system preference out of the box. To pin or toggle the mode, set <code>color-scheme</code> (and optionally a <code>[data-theme]</code> attribute) on a wrapper. The opt-in theme is intentionally outside the Safari 16.5 baseline for core table behavior; Safari 16.5 consumers can theme through their own <code>--nat-table-*</code> tokens instead of importing <code>theme.css</code>.</p>\n<p>Token precedence is unchanged: a <code>--nat-table-*</code> token set on any ancestor wrapper (or on the surface element itself) still wins over the opt-in theme, and derived stock values (borders, dividers, mixed colors) recompute from your overridden palette tokens. If you inspect computed styles you may see internal <code>--sys-nat-table-*</code> bridge variables — they are implementation detail; never set them, set the matching <code>--nat-table-*</code> token instead.</p>\n<p>Product UI should scope tokens on a wrapper or <code>NatTableSurface</code> ancestor so all companion controls inherit the same theme, whether or not the opt-in stylesheet is imported.</p>\n<p>The tables below list the main stock values and resolved core defaults. Use them as a reference if you want to replicate the stock look, override individual tokens on top of it, or build your own theme from scratch without importing <code>theme.css</code> at all.</p>\n<h3>Palette And Core</h3>\n<p>The palette is light/dark responsive via <code>light-dark(&lt;light&gt;, &lt;dark&gt;)</code>.</p>\n<table>\n<thead>\n<tr>\n<th>Token</th>\n<th>Stock value or resolved default</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>--nat-table-font-family</code></td>\n<td><code>&#39;Inter&#39;, -apple-system, blinkmacsystemfont, &#39;Segoe UI&#39;, roboto, helvetica, arial, sans-serif</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-color-text</code></td>\n<td><code>light-dark(#0f1419, #e6eaef)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-color-text-muted</code></td>\n<td><code>light-dark(#5b6672, #9aa4b1)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-color-accent</code></td>\n<td><code>light-dark(#1f6feb, #4f8bff)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-color-success</code></td>\n<td><code>light-dark(#127a3b, #3dd68c)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-color-warning</code></td>\n<td><code>light-dark(#8f5f00, #e5b452)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-color-danger</code></td>\n<td><code>light-dark(#b42318, #f15b5b)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-color-surface</code></td>\n<td><code>light-dark(#fff, #14171c)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-color-surface-elevated</code></td>\n<td><code>light-dark(#fff, #14171c)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-color-surface-sticky</code></td>\n<td><code>light-dark(#f4f5f7, #1a1e24)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-color-border</code></td>\n<td><code>light-dark(#e4e7eb, #262b33)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-color-divider</code></td>\n<td><code>light-dark(#e4e7eb, #262b33)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-region-background</code></td>\n<td><code>var(--nat-table-color-surface)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-header-background</code></td>\n<td><code>var(--nat-table-color-surface-sticky)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-header-color</code></td>\n<td><code>var(--nat-table-color-text-muted)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-cell-border-color</code></td>\n<td><code>var(--nat-table-color-divider)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-cell-color-positive</code></td>\n<td><code>var(--nat-table-color-success)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-cell-color-negative</code></td>\n<td><code>var(--nat-table-color-danger)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-cell-color-warning</code></td>\n<td><code>var(--nat-table-color-warning)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-cell-color-neutral</code></td>\n<td><code>var(--nat-table-color-text-muted)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-empty-state-color</code></td>\n<td><code>var(--nat-table-color-text-muted)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-loading-state-color</code></td>\n<td><code>var(--nat-table-empty-state-color)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-error-state-color</code></td>\n<td><code>var(--nat-table-cell-color-negative)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-focus-ring-color</code></td>\n<td><code>var(--nat-table-color-accent)</code></td>\n</tr>\n</tbody></table>\n<h3>Controls</h3>\n<table>\n<thead>\n<tr>\n<th>Token</th>\n<th>Stock value or resolved default</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>--nat-table-radius-card</code></td>\n<td><code>12px</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-radius-region</code></td>\n<td><code>8px</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-radius-input</code></td>\n<td><code>6px</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-radius-chip</code></td>\n<td><code>6px</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-space-card</code></td>\n<td><code>18px 22px</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-space-card-compact</code></td>\n<td><code>14px 16px</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-space-control-block-gap</code></td>\n<td><code>12px</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-space-chip-row-gap</code></td>\n<td><code>10px</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-chip-background</code></td>\n<td><code>var(--nat-table-color-surface)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-chip-background-active</code></td>\n<td><code>color-mix(in srgb, var(--nat-table-color-accent) 12%, var(--nat-table-color-surface))</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-chip-border-color</code></td>\n<td><code>var(--nat-table-color-border)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-pager-background</code></td>\n<td><code>var(--nat-table-color-surface)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-pager-border-color</code></td>\n<td><code>var(--nat-table-color-border)</code></td>\n</tr>\n</tbody></table>\n<h3>Layout And Stacking</h3>\n<table>\n<thead>\n<tr>\n<th>Token</th>\n<th>Stock value or resolved default</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>--nat-table-region-overflow-x</code></td>\n<td><code>auto</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-region-overflow-y</code></td>\n<td><code>auto</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-region-overscroll-behavior</code></td>\n<td><code>none</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-region-overscroll-behavior-x</code></td>\n<td><code>none</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-region-overscroll-behavior-y</code></td>\n<td><code>none</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-max-height</code></td>\n<td><code>inherit</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-min-height</code></td>\n<td><code>auto</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-state-min-height</code></td>\n<td><code>var(--nat-table-min-height, 0)</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-height</code></td>\n<td><code>inherit</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-sticky-top</code></td>\n<td><code>0</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-z-index-sticky-header</code></td>\n<td><code>4</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-z-index-pinned-cell</code></td>\n<td><code>5</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-z-index-pinned-header</code></td>\n<td><code>6</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-z-index-focus-cell</code></td>\n<td><code>7</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-z-index-resize-handle</code></td>\n<td><code>8</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-z-index-resize-guide</code></td>\n<td><code>9</code></td>\n</tr>\n<tr>\n<td><code>--nat-table-z-index-drag-preview</code></td>\n<td><code>12</code></td>\n</tr>\n</tbody></table>\n<h2>Core-Only Tables</h2>\n<p>The core table is unstyled by default whether or not you use <code>NatTableSurface</code> — with or without the surface, it renders with system-color fallbacks unless you import the opt-in theme or provide tokens yourself. For a polished core-only table, import <code>ng-advanced-table/components/theme.css</code> or provide the core tokens yourself.</p>\n<pre><code class="language-html">&lt;section class=&quot;plain-table&quot;&gt;\n  &lt;nat-table [data]=&quot;rows()&quot; [columns]=&quot;columns&quot; accessibleName=&quot;Orders&quot; /&gt;\n&lt;/section&gt;\n</code></pre>\n<pre><code class="language-css">.plain-table {\n  --nat-table-color-text: #111827;\n  --nat-table-region-background: #ffffff;\n  --nat-table-region-border-color: #e5e7eb;\n  --nat-table-header-background: #f9fafb;\n  --nat-table-header-color: #4b5563;\n  --nat-table-cell-border-color: #e5e7eb;\n  --nat-table-row-background-hover: #f3f4f6;\n  --nat-table-focus-ring-color: #2563eb;\n}\n</code></pre>\n<h2>Semantic Cell Tones</h2>\n<p>Use <code>meta.cellTone</code> for semantic coloring and keep the meaning available in text or context.</p>\n<pre><code class="language-ts">{\n  accessorKey: &#39;changePercent&#39;,\n  header: &#39;Chg %&#39;,\n  meta: {\n    label: &#39;Change percent&#39;,\n    align: &#39;end&#39;,\n    cellTone: (context) =&gt; {\n      const value = context.getValue&lt;number&gt;();\n\n      if (value &gt; 0) return &#39;positive&#39;;\n      if (value &lt; 0) return &#39;negative&#39;;\n\n      return &#39;neutral&#39;;\n    },\n  },\n  cell: (context) =&gt; `${context.getValue&lt;number&gt;().toFixed(2)}%`,\n}\n</code></pre>\n<p>Then theme the tone tokens.</p>\n<pre><code class="language-css">.orders-table-theme {\n  --nat-table-cell-color-positive: #166534;\n  --nat-table-cell-color-negative: #b91c1c;\n  --nat-table-cell-color-warning: #a16207;\n  --nat-table-cell-color-neutral: #475569;\n}\n</code></pre>\n<p>Do not encode business meaning only through color. Include signs, labels, icons with text alternatives, or nearby text that makes the state clear.</p>\n<h2>Theming Checklist</h2>\n<ul>\n<li>Put tokens on a wrapper, feature shell, or app theme root.</li>\n<li>Prefer <code>--nat-table-*</code> tokens for new themes.</li>\n<li>Keep table, companion controls, and render metrics under the same theme scope.</li>\n<li>Use <code>color-scheme</code> for light and dark scopes.</li>\n<li>Verify focus-visible states after changing focus, border, header, chip, pager, or semantic colors.</li>\n<li>Keep text, controls, focus indicators, pinned dividers, and semantic tones at WCAG AA contrast.</li>\n<li>Do not target private component classes, generated DOM structure, Angular internals, or <code>::ng-deep</code>.</li>\n</ul>\n',
    headings: [
      {
        depth: 2,
        id: 'recommended-shape'
      },
      {
        depth: 2,
        id: 'theme-scope'
      },
      {
        depth: 2,
        id: 'core-table-tokens'
      },
      {
        depth: 2,
        id: 'ui-control-tokens'
      },
      {
        depth: 2,
        id: 'opt-in-stock-theme'
      },
      {
        depth: 3,
        id: 'palette-and-core'
      },
      {
        depth: 3,
        id: 'controls'
      },
      {
        depth: 3,
        id: 'layout-and-stacking'
      },
      {
        depth: 2,
        id: 'core-only-tables'
      },
      {
        depth: 2,
        id: 'semantic-cell-tones'
      },
      {
        depth: 2,
        id: 'theming-checklist'
      }
    ]
  },
  '/docs/toolbar-actions.md': {
    html: '<h2>Toolbar Shell</h2>\n<p>Use <code>&lt;nat-table-toolbar&gt;</code> for generic command rows near a table. Projected controls that participate in toolbar keyboard navigation must use <code>natToolbarItem</code> or <code>NatToolbarGroup</code>.</p>\n<p>Keep DOM order aligned with screen-reader and roving-keyboard order. Visual placement should not make keyboard navigation surprising.</p>\n<h2>Table Actions</h2>\n<p>A Table Action is a user-triggered operation that acts on table rows or table presentation state. Toolbar placement is optional; the behavior is not defined by where the control is rendered.</p>\n<h2>Keyboard Order</h2>\n<p>Toolbar items use roving focus. Put controls in the order users should encounter them, then use toolbar positions for visual grouping. Do not place standalone composite controls inside the toolbar unless their individual interactive elements register correctly.</p>\n<h2>Common Uses</h2>\n<p>Good toolbar content includes refresh buttons, export buttons, column visibility controls, pagination controls, and app-owned bulk actions. Product-specific search and domain filters can sit in or near the toolbar when they fit the workflow.</p>\n',
    headings: [
      {
        depth: 2,
        id: 'toolbar-shell'
      },
      {
        depth: 2,
        id: 'table-actions'
      },
      {
        depth: 2,
        id: 'keyboard-order'
      },
      {
        depth: 2,
        id: 'common-uses'
      }
    ]
  }
} as const satisfies Readonly<Record<string, DocsHtmlEntry>>;

export const findDocsHtmlEntry = (markdownPath: string): DocsHtmlEntry | undefined => {
  return (docsHtmlRegistry as Readonly<Record<string, DocsHtmlEntry>>)[markdownPath];
};
