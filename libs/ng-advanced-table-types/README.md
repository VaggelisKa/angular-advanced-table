# ng-advanced-table-types

Internal plain TypeScript table contract source for the `angular-advanced-table` workspace.

This library is not an Angular package and is not published. Public packages use it for compile-time drift checks, but emitted package declarations must not import from `ng-advanced-table-types`.
