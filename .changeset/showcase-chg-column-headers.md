---
'ng-advanced-table': patch
---

Showcase app: rename move column headers from `24h $` / `24h %` to `Chg $` / `Chg %` so the '24h' label is not repeated on adjacent columns in the live market demo.
