---
'ng-advanced-table': patch
'ng-advanced-table-ui': patch
---

Refine column reordering so draggable headers can be grabbed from the full header surface instead of a small dedicated handle. Drag previews now inherit the active table theme more faithfully, and the built-in sort and pin buttons continue to behave like buttons while reordering is enabled.
