---
"ng-advanced-table": patch
---

Add a table options dialog to the showcase page so users can toggle capabilities such as column pinning, column reordering, pagination, search, column visibility controls, and render metrics to quickly test simpler table variants.
